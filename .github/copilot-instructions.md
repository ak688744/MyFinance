# Project Decisions & Context
<!-- Auto-generated by project-memory. Do not edit manually. -->
<!-- Regenerate with: npx project-memory sync -->

## Architecture
- Portfolio calculations use transactions-only, holdings for metadata — portfolioService.ts rewritten to derive holdings/summary/returns entirely from investment_transactions + latest NAV. investment_holdings table is still written (archive) but never read for calculations. Period view uses cohort math: invested-in-period, current value of those units, XIRR over window cash flows. ALL = lifetime.
- AI feature uses user-provided API keys with multi-provider support — Users provide their own API keys (Claude/Gemini/OpenAI). Keys stored locally on device (SecureStore). App calls LLM APIs directly with tool_use for financial queries over local SQLite data. No backend proxy needed. Tool layer structured for future MCP server extraction.
- AI v1 scope: Gemini provider, model selection, token cost display, no history — v1 starts with Gemini only. Users pick model (Flash/Pro). No conversation persistence — fresh each session. Show token cost on screen (input/output tokens × model pricing). Tool layer is provider-agnostic for future Claude/OpenAI addition.
- MyFinance Web re-platform: Node/TS monorepo, Claude Agent SDK, MCP, SQLite→Postgres via Drizzle — Re-platforming the local-first RN app into a self-hosted (single-user, auth later) wealth-manager. Stack locked: Node+TypeScript backend (ports existing features/ as-is); Claude Agent SDK for the agent orchestrator + specialist agents; MCP server as the tool contract over finance data; Drizzle ORM targeting SQLite now → Postgres+pgvector later (RAG vectors in same DB). Monorepo: packages/core (ported features/ + schema + types), packages/api, packages/mcp, packages/agents, apps/web, apps/mobile. The AI agent layer is the product; backend/UI/MCP exist to feed it. Bottom-up build order L0 Foundation → L1 Ingestion+API → L2 Web UI → L3 MCP → L4 AI. Full plan: docs/superpowers/MASTER_PLAN.md.
- L0 keeps money as REAL/float (paise rejected) and uses repository-over-Drizzle seam — L0 Foundation decisions (spec docs/superpowers/specs/2026-06-12-l0-foundation-design.md): (1) Money stays REAL/float, faithful port — integer-paise rejected because for single-user/single-portfolio the only benefit (exact aggregates) is cosmetic (hidden by toFixed(2)) and a rupee->paise rounding step would risk diverging from already-Groww-validated numbers. Revisit as clean migration if drift bites. (2) Data layer = repository modules own ALL Drizzle/SQL; pure domain/ logic (xirr, returns, portfolio, categorize) depends on injected repo INTERFACES, never Drizzle — enables fake-repo unit tests + isolates Postgres swap to client.ts+schema.ts. (3) Scope shift: import parsers+orchestration ported into core in L0; L1 becomes the server-side ingestion API over them. (4) better-sqlite3 driver isolated in db/client.ts. (5) Fastify read-only API skeleton. Build via subagent-driven-development on branch layer/0-foundation.
- L0 returns/portfolio port unifies XIRR terminal-flow date on LOCAL time (formatDate), server TZ matters — Task 3.2 port: the RN originals were inconsistent — portfolioService used UTC (new Date().toISOString().slice(0,10)) for terminal XIRR cash-flow dates, while returnsCalculator.getPeriodReturns used LOCAL time (formatDate via getFullYear/getMonth/getDate). The port (packages/core/src/domain/{returns,portfolio}.ts) unified on LOCAL formatDate everywhere. RATIONALE: getPeriodReturns is the function under the Groww golden-master test, so preserving its local-time behavior exactly is correct; portfolioService's UTC was the inconsistency. Spec-review (feature-dev:code-reviewer) confirmed all other 16 logic points are faithful ports. KNOWN ITEM (not a bug): local-time formatDate means cash-flow/terminal dates depend on the SERVER's timezone. For this India-based single-user wealth manager, deploy with TZ=Asia/Kolkata (IST) so dates match the user's expectation and Groww. Impact on XIRR is sub-day/immaterial over year-scale windows regardless. `today` is injectable (default new Date()) for deterministic tests. Revisit if multi-TZ ever matters. Decision id from subagent: 2198ce4c (its own save).
- Categorization engine ported to packages/core/src/domain/categorize.ts (pure verbatim + repo-injected sync) — Ported categorizeTransaction.ts + manageRules.ts into categorize.ts. Pure matchers (extractMerchantKey/extractUpiNoteKeyword/createCategorizationInput/resolveCategoryFromRules/builtinRules + helpers) are byte-identical to source; only change is resolveCategoryFromRules reads camelCase StoredCategoryRule fields (ruleType/patternValue/categoryId) instead of snake_case. DB functions (recategorizeNonManualTransactions/saveCategoryMemoryRule/createRule/updateRuleCategory/deleteRule) take injected {ruleRepo,txRepo}; synchronous (better-sqlite3). recategorize re-derives input from description (not stored merchant_key/upi columns), matching source. Extended CategoryRuleRepo.updateRuleCategory(ruleId,categoryId,ruleType?,priority?) in types.ts+categoryRuleRepo.ts to faithfully set rule_type+priority(merchant?200:100). Category CRUD (create/rename/delete) DEFERRED — needs CategoryRepo extension, noted in file. Commit ae7e0b0 on layer/0-foundation. 119 tests green, typecheck clean.
- Import orchestration ported to packages/core with injected runInTransaction runner + injectable amfiMatch — Task 5.2: the 3 import orchestrators (importTransactions HDFC expense, importHoldings, importInvestmentTransactions) now live in packages/core/src/import/, repo-injected (no drizzle/schema imports). Transactions: each fn takes deps.runInTransaction<T>(fn)=>T built by the caller from the better-sqlite3 handle's .transaction(); per-row insert loops + deletes run inside it for atomicity. importTransactions and importInvestmentTransactions are synchronous-bodied but return Promise (signature parity); importHoldings stays truly async because the post-import AMFI auto-match is network. AMFI network call is an injectable dep amfiMatch?({schemeRepo}) defaulting to domain/amfiMatcher.autoMatchAmfiCodes; tests inject a stub returning {matched:0,total:0,matches:[]} so no network. settleDelayMs (default 1000) also injectable. New repo methods added: HoldingsRepo.deleteByAccountAppDate, InvestmentTxRepo.deleteByAccountAppDateRange, ImportHistoryRepo.findInvestmentImports/deleteInvestmentImport, ExpenseTransactionRepo.insertIgnore (onConflictDoNothing == INSERT OR IGNORE on UNIQUE dedupe_key). createInvestmentImport already supported holdings totals — no extension needed. Source strategy preserved verbatim: expense import inserts import_history + reads rules OUTSIDE the tx then loops inside; holdings find-existing/delete-holdings/delete-history/insert-new/match-or-create order; investment-tx pre-flight scheme resolution returns unmatched_schemes (sorted) WITHOUT touching DB. 167 core tests pass, typecheck clean. Commit 7523a85.
- packages/api L0: Fastify read-only API wires repos+domain via direct decoration; nav from core, no-amfi fixtures keep tests offline — packages/api (L0 read-only skeleton) is built. buildServer(opts) returns a non-listening Fastify instance (logger:false) used in tests via app.inject(). DB wiring: registerDb(app,dbPath) calls core runMigrations(dbPath) and decorates app.db/app.sqlite/app.repos DIRECTLY (not app.register) so decorations live on the root instance (avoids encapsulation scoping); onClose hook closes sqlite. FastifyInstance is type-augmented (declare module 'fastify') with db/sqlite/repos; sqlite type is derived as ReturnType<typeof runMigrations>['sqlite'] to avoid a direct better-sqlite3 dep in api. Endpoints: GET /health -> {data:{status:'ok',db}} (SELECT 1 probe); GET /transactions?limit&offset&categoryId -> {data:rows} via expenseTxRepo.list; GET /investments/summary -> {data:summary} via getPortfolioSummary({txRepo,nav}); GET /investments/returns?period -> {data:returns} via getPeriodReturns({txRepo,schemeRepo,holdingsRepo,nav}), invalid period throws Error w/ statusCode=400. Error handler returns {error:{message,statusCode}} (default 500, respects err.statusCode). NAV dependency is built from core getLatestNAV/getNAVForDate but is NEVER called in tests because fixtures seed schemes with NULL amfi_code -> domain falls back to invested/holdings value (offline, deterministic). NO write/import endpoints (that's L1).
- L0 Foundation COMPLETE + PR #1 opened; L1 (ingestion API) is next layer — L0 Foundation layer DONE 2026-06-12. Branch layer/0-foundation (29 commits) pushed to origin (ak688744/MyFinance); PR #1 open into main: https://github.com/ak688744/MyFinance/pull/1. MASTER_PLAN §4 L0 row = ✅.

Delivered: pnpm monorepo (core+api built, mcp/agents/web/mobile stubbed, Node 20 via .nvmrc); packages/core (Drizzle 8-table schema REAL money + CHECKs + migrations + seed; 7 repos owning all SQL; domain port xirr-verbatim/returns/portfolio/categorize/nav/amfi; import parsers+orchestration; public index.ts); packages/api (Fastify read-only /health, /transactions, /investments/summary, /investments/returns via inject tests). 175 tests green (core 170 + api 5). Typecheck via `tsc --build` (NOT plain --noEmit — composite project refs need core's .d.ts emitted first; research f275d339). Seam invariant verified (no drizzle/driver import in domain/import). Final code review READY TO MERGE.

T1 GATE PASSED: Groww golden-master per-scheme XIRR within 0.046pp worst-case (6 schemes, 104 real txns).

GH AUTH NOTE: two gh accounts logged in — vkhandelwal-coursera (no push rights to this repo) and ak688744 (repo owner). Must `gh auth switch --user ak688744` to push/PR here.

NEXT = L1 (Ingestion + Data API): expose server-side import endpoints (POST) over the already-ported core import logic (importTransactions/importHoldings/importInvestmentTransactions), dedupe, categorize; expand the read API for UI + agents. New T1 session: read MASTER_PLAN, classify tier, branch layer/1-ingestion.
- L1 Ingestion + Data API COMPLETE — PR #2 open; next layer is L1.5 (merge PR #2 first) — L1 (Ingestion + Data API) built and finished 2026-06-13 on branch layer/1-ingestion (11 commits off main), PR #2 open into main: https://github.com/ak688744/MyFinance/pull/2. Built via superpowers:subagent-driven-development (fresh subagent per task across the 10-task plan docs/superpowers/plans/2026-06-13-l1-ingestion.md; spec docs/superpowers/specs/2026-06-12-l1-ingestion-design.md), with a spec-compliance review on the integration task and a final feature-dev:code-reviewer pass (verdict READY TO MERGE, zero blocking).

DELIVERED (packages/api HTTP layer over core, NO new package):
- plugins/txRunner.ts: makeRunInTransaction(app.sqlite) => <T>(fn)=>T (sqlite.transaction(fn)()), feeds core import orchestrators.
- lib/multipart.ts: readMultipart(req) => {file:{buffer:ArrayBuffer,filename}|null, fields} via @fastify/multipart@^9 (first-file-wins; buffer sliced into standalone ArrayBuffer for XLSX type:'array'). server.ts registers multipart with limits {fileSize:25MB, files:1}.
- import/registry.ts: resolveParser(platform, kind) — REGISTRY {hdfc:{expense:parseHdfcStatementXls}, groww:{holdings:parseGrowwHoldingsXls, transactions:parseGrowwTransactionXls}}; case-insensitive platform; BARE function refs (test asserts .toBe); unsupported pair throws statusCode-400. D6 seam — ET Money etc. drop-in with zero route/orchestrator change.
- routes/imports.ts: POST /imports/expenses (platform default hdfc), /imports/investments/holdings, /imports/investments/transactions (default groww) + GET /imports. D1 server-parses-upload; parser throw caught->400; missing file/field->400. holdings/tx REQUIRE accountName+investmentApp (D7). investment-tx returns BOTH {status:'success',...} and {status:'unmatched_schemes',unmatchedSchemes[]} as 200 (D2). Wiring: expenses uses expenseTxRepo+categoryRuleRepo; investment-tx uses txRepo (investment); amfiMatch injected (defaults core autoMatchAmfiCodes).
- routes/categories.ts: GET /categories; POST(201)/PATCH/DELETE /categories/rules; POST /recategorize. createRule empty-pattern->400 (core throws 'Rule pattern cannot be empty.'). create/update/delete each re-run recategorize internally (core behavior, no extra calls).
- routes/investments.ts EXPANDED: GET /investments/holdings (+?account, sortBy, sortOrder), /allocation (+?account), /accounts (distinct names, D7). getHoldings/getAssetAllocation async; getAccounts sync.
- buildServer({dbPath, amfiMatch}) — amfiMatch:AmfiMatch (type exported from routes/imports.ts) lets tests inject no-network stub returning {matched:0,total:0}. registerDb now calls seedDatabase(db) so starter categories exist on fresh DB.

ONE core change (non-financial, NO Groww re-validation): ImportHistoryRepo.listAll() in repositories/importHistoryRepo.ts + ImportRecord type in repositories/types.ts (also now exported from core index.ts). Unifies import_history (expense) + investment_import_history rows into ImportRecord[] {kind:'expense'|'investment', id, sourceName, importType, recordCount, accountName, investmentApp, importedAt}, JS-side sort importedAt desc -> kind -> id. No XIRR/returns/portfolio/categorize/NAV math touched.

VERIFICATION: core 172 tests green (170 prior + 2 listAll), api 28 green (5 prior + txRunner 2 + registry 3 + imports 7 + categories 6 incl PATCH + investments-reads 4 + a corrupt-file-400 + the 4 read-endpoints now actually running). Typecheck clean via tsc --build (api->core project ref; build core FIRST). Seam invariant: zero real drizzle/better-sqlite3 imports in core domain/ or import/ (only architecture comments). Smoke-run on-disk server OK (/health, real curl multipart upload imported 3 txns, /imports normalized shape, /categories seeded).

GOTCHAS confirmed this build: (1) @fastify/multipart NOT auto-augmenting unless dep installed — installed ^9.4.0. (2) better-sqlite3 + @types/better-sqlite3 added as api DEVdeps because txRunner.test.ts imports better-sqlite3 directly (Sqlite type itself comes from db.ts as ReturnType<typeof runMigrations>['sqlite'], no direct dep in src). (3) Adding seedDatabase to registerDb collided with read-endpoints.test.ts manual 'food' category INSERT -> changed to INSERT OR IGNORE. (4) fixtures are PII-free synthetic .xls generated by test/fixtures/makeFixtures.ts (force-added; .xls gitignored) — layouts verified against parser header-alias detection via a sanity gate before routes built. (5) push/PR via gh auth switch --user ak688744.

NEXT = merge PR #2 to main (delete branch), then L1.5 — Unified Investment Model + first-class Accounts (all asset classes MF/stocks/PPF/FD/cash; two axes ingestion file-import-vs-manual + valuation market/computed/manual; also makes accounts first-class for EXPENSES). Reshapes core schema => T1 WITH Groww re-validation. See MASTER_PLAN §4 L1.5 row + the prior L1.5 decision.
- L1.5 Unified Investment Model: three families, MF-untouched union, read-time projection, rate-schedule, derive-on-read history — L1.5 brainstorm+planning DONE 2026-06-14 (T1, on main, docs-only). Spec: docs/superpowers/specs/2026-06-14-l1.5-unified-investment-model-design.md (commit da7ed7f). Plan: docs/superpowers/plans/2026-06-14-l1.5-unified-investment-model.md (30 TDD tasks, fully self-contained). NO code yet — execute in a new session via subagent-driven-development, branch layer/1.5-unified-investment.

ORGANIZING INSIGHT — THREE STRUCTURAL FAMILIES (not 2):
- Family 1 (transactional/market): MF now (stocks DEFERRED). Transaction stream, units×price, XIRR. Stays in existing investment_schemes/investment_transactions; portfolio.ts UNCHANGED. Stocks will EXTEND this pipeline (scheme→instrument), NEVER the assets table.
- Family 2 (single-value): FD (computed, one deposit) + gold/real_estate/cash (manual stated value).
- Family 3 (contributory/computed): PPF/EPF/NPS — RECURRING contributions to same account, no units/NAV, rate may change over time. User flagged this: FD is one-time but PPF/EPF/NPS keep receiving deposits.

LOCKED DECISIONS (D1-D11):
- D1 v1 scope: implement MF(market) + manual(gold/RE/cash) + computed(FD/PPF/EPF/NPS) + loans(amortization). DEFER live stock/gold-ETF pricing (no new price provider). assetClass enum OPEN.
- D2 MF data path: keep MF tables as-is, portfolio.ts XIRR math UNCHANGED — lowest Groww re-validation risk. Net-worth rollup UNIONS MF path + new classes.
- D3 global assets surface: READ-TIME UNIFIED PROJECTION (getAllAssets) — physical assets table is Family 2/3 only; MF is projected in at read time (MF value/units/XIRR always derived, never stored, so no MF rows in assets — avoids duplication+staleness+import-sync). User explicitly asked if MF/stocks fit in assets table; answer: MF projected, stocks extend MF pipeline.
- D4 net-worth history: DERIVE-ON-READ (no snapshot table). Current NW = must-have; trend series = STRETCH. MF history needs getNAVForDate (already exists on NavLookup); manual assets need asset_valuations time-series; computed deterministic by formula.
- D5 new-math validation: TEXTBOOK-FORMULA unit tests (EMI/amortization, compound-interest/FD-maturity). No external oracle for these. Groww golden-master still guards MF/XIRR.
- D6 rate over time: RATE SCHEDULE (asset_rates: effective_from+rate periods); compound each contribution under the rate in effect per period. FD=1 period; PPF multi (govt resets).
- D7 contribution shape: asset_contributions stream. FD=1 row, PPF/EPF/NPS=N rows, manual=0.
- D8 accounts: first-class, PER-DOMAIN (investment/expense), two-level (institution,label) so MULTIPLE GROWW ACCOUNTS = same institution distinct label (matches existing accountName/investmentApp grouping in portfolio.ts GroupKey). INCLUDES expense accounts this layer (nullable account_id on transactions).
- D9 AI insight slots DEFERRED to L4. D10 budgets DEFERRED to L2.
- D11 NO data migration (no prod data yet) — pure schema evolution: 6 new tables (accounts, assets, asset_contributions, asset_rates, asset_valuations, liabilities) + transactions.account_id. Groww gate = standing re-validation INVARIANT not data-preservation.

NEW TABLES: accounts, assets(account_id, asset_class CHECK, valuation_strategy computed|manual, ingestion_mode, params JSON, status), asset_contributions, asset_rates, asset_valuations, liabilities(principal/annual_rate/tenure_months|emi_amount/start_date, CHECK tenure-or-emi). Money/rate/value all REAL (L0 decision). New domain modules: domain/valuation/{computed,manual,index}.ts, domain/loans/amortization.ts, domain/networth/networth.ts (getAllAssets/getNetWorth/getNetWorthHistory, repo+getMfHoldings injected). New repos: account/asset/assetContribution/assetRate/assetValuation/liability. New API routes: /networth(+/history), /assets(+/:id/contributions|valuations|rates), /accounts, /liabilities; MF import ensureAccount (non-financial).

T1 GATE: existing Groww golden-master + full core suite pass UNCHANGED (MF pipeline untouched) + new rollup-continuity test (MF-only getNetWorth.totalAssets == getPortfolioSummary.totalCurrentValue). After L1.5 -> L2 (Web UI). Related decisions: L1.5 model/sequencing 437133fd, frontend spike c5a4e601, L1 complete ab012766.
- L1.5 Task 3: domain types appended to packages/core/src/types.ts (Account/Asset/Liability/ValuedAsset/NetWorth*) — L1.5 Task 3 added all unified-investment domain types to the END of packages/core/src/types.ts (appended, no existing type modified; zero name collisions verified before edit). Types: AccountDomain, Account, AssetClass (ppf/epf/nps/fd/gold/real_estate/cash), ValuationStrategy (market|computed|manual), IngestionMode (manual_entry|file_import), CompoundingFrequency, AssetParams (JSON params with compounding/maturityDate/payout/grams + index signature), Asset (valuationStrategy narrowed to computed|manual — market is MF-only/projected, never an assets-table row), AssetContribution, AssetRate (annual %), AssetValuation, Liability (loanType home|car|personal|other, tenureMonths|emiAmount nullable), ValuedAsset (uniform projection shape; assetId null for MF, assetClass 'mutual_fund' for MF), AmortizationRow, LoanStatus, NetWorthClassBreakdown, NetWorthSummary, NetWorthPoint. All money/rate/value are REAL number (L0 decision). These feed Tasks 4-19 (valuation engines, repos, net-worth rollup).
- L2 Web UI: apps/web Vite/React/TS SPA over REST API; read-only /expenses endpoints; build complete on layer/2-web-ui — L2 (Web UI) built 2026-06-15 via subagent-driven-development on branch layer/2-web-ui (off main, ~24 commits). T1 but NO Groww re-validation needed beyond the standing gate (no core financial logic touched — verified golden-master 6/6 unchanged; core 222 + api 45 + web 15 green).

STACK (decided): new apps/web package = Vite + React 19 + TypeScript + Tailwind + TanStack Query + React Router + Recharts. PURE consumer of the Fastify REST API — no business logic in the browser (all money math stays in core/API). Imports @myfinance/core for shared TYPES only. Vite dev proxies /api/*→http://localhost:${API_PORT:-3001} (no CORS plugin needed); VITE_API_BASE defaults to /api.

ARCHITECTURE (Approach A): feature-folder (features/{networth,investments,expenses,loans,assistant}) + thin typed API client (lib/apiClient.ts {data}-envelope unwrap + ApiError; lib/hooks.ts TanStack Query resource+mutation hooks; lib/queryKeys.ts) + shared UI kit (components/ui/* — Card/KPIStat/Badge(MARKET|COMPUTED|MANUAL)/DataState/AIInsightCard/Modal + Recharts wrappers Trend/Donut/SpendBar). Pure logic isolated + TDD'd in lib/format.ts (INR lakh/crore, compact ₹1.84Cr/₹11.8L, percent, date) and lib/transforms.ts (groupHoldingsByClass, allocationToChartData, summaryByCategoryWithNames).

SCREENS: Net Worth (hero+composition donut+Assets−Liab KPIs+trend), Investments (KPI strip incl XIRR + asset-class group cards w/ valuation badges) + Investment Analyzer (single-holding), Expenses (Spent/Income/Saved KPIs + donut + MoM bars + txn list), Loans (cards + amortization drawer), Assistant placeholder. WRITE FLOWS built: Add-investment (computed/manual per-class form), Add-account, Add-loan (tenure-based), file-import upload (multipart).

ONE API ADDITION (read-only, no Groww re-validation): GET /expenses (filterable from/to/direction/search/categoryId/accountId/limit/offset) + GET /expenses/summary (totalSpent/totalIncome/saved + byCategory + byMonth) over new ExpenseTransactionRepo.query()/summary() repo methods — pure SQL aggregation in the repo layer (L0 seam), debit-only spend grouping; no XIRR/portfolio/categorize/NAV.

KEY DECISIONS: charts use REAL data only — honest empty hints where MF historical NAV is unwired (no fabricated data, faithful-reporting). Budgets DEFERRED (spike §6.9). AI cards static + disabled "Coming in L4" (L4 seam reserved, no agent calls). Testing = TDD on endpoints+lib pure logic + UI-kit smoke test; LIGHT frontend tests + a written manual browser test plan (apps/web/MANUAL_TEST_PLAN.md) — user's explicit choice (verifies UI in-browser via computer-use plugin).

DEFERRED (logged, not built): edit-transaction-category UI (needs a NEW per-transaction category endpoint touching categorization → its own T1); MF historical-NAV wiring (trend-chart follow-up); live stock/gold pricing; Add-loan EMI-mode (tenure-only v1); budgets.

GOTCHAS: apps/web/tsconfig keeps composite:true+noEmit:true as a leaf referencing packages/core — build core first (tsc --build) so .d.ts resolves; local *.db now gitignored. NEXT = push layer/2-web-ui + PR (gh auth switch --user ak688744), merge, then L3 (MCP Server). Spec docs/superpowers/specs/2026-06-14-l2-web-ui-design.md, plan docs/superpowers/plans/2026-06-14-l2-web-ui.md.
- L2 categorization/rules Expenses UX: editable chips + learn prompt + manage panel; T2, no categorize algorithm change — Added the Expenses categorization/rules UX that L2 had deferred (2026-06-15, T2, same branch layer/2-web-ui / PR #5). Classified T2 (not T1) because it only EXPOSES the existing tested categorization engine via new endpoints + UI — domain/categorize.ts is BYTE-IDENTICAL. T2 gate held: Groww golden-master 6/6 unchanged, core 227 + api 52 + web 15 green, typecheck clean.

BACKEND (additive only): CategoryRepo gained create/rename/delete/exists — DELETE semantics (user-chosen): reassign tagged transactions to category_id=NULL + DELETE dependent category_rules + DELETE category, run as 3 ordered drizzle statements (foreign_keys=ON requires null-out + delete-children BEFORE delete-parent; non-transactional, accepted for low-volume user CRUD). New API: GET /categories/rules (list via getActiveRules), POST /categories {name,icon?} (slugifyCategoryName id, 201, 409 if exists), PATCH/DELETE /categories/:id (404 if missing), PATCH /transactions/:id/category {categoryId:string|null, createRuleMerchant?:boolean} (updateCategory source='manual'; if createRuleMerchant+non-null → extractMerchantKey(description) → saveCategoryMemoryRule(merchant) → recategorizeNonManualTransactions). ExpenseTransactionRepo.getById added (404 + description for merchant-key). GET /categories/rules MUST be registered BEFORE /categories/:id (Fastify path collision). slugifyCategoryName/extractMerchantKey/saveCategoryMemoryRule re-exported from core index.

FRONTEND: CategoryChip per txn (click→category picker→reassign; null categoryId rendered as flagged amber dashed "Uncategorized" = the work-list). Learn prompt: on reassign, one-off assignment fires FIRST (createRuleMerchant absent), THEN inline "Always categorize <merchant> as X?" Yes/No — Yes re-calls with createRuleMerchant:true (creates merchant rule + recategorizes), No dismisses. ManageCategoriesModal: categories CRUD (add/rename/delete-with-confirm) + rules CRUD (add/edit-category/delete) + "Recategorize all" (POST /recategorize). Hooks: useRules/useUpdateTxCategory/useCreateCategory/useRenameCategory/useDeleteCategory/useCreateRule/useUpdateRule/useDeleteRule/useRecategorize.

KEY DESIGN INSIGHT: the categorization engine is DETERMINISTIC rule-matching (no confidence score) — every txn is already auto-categorized at import via the priority ladder merchant_rule(200)>upi_note_keyword(100)>builtin_rule>null, with category_source recording HOW. So the mockup's per-row "✓/✗ is this right?" was DROPPED (would ask you to confirm hundreds of confidently-matched rows); replaced with editable chips on every row + visually-flagged uncategorized rows as the real work-list. extractMerchantKey only derives keys from STRUCTURED bank formats (UPI-/ACH/POS), so the learn prompt only creates a rule when a merchant key is derivable (plain descriptions like "SWIGGY ORDER 123" yield null → no rule, by design). LIVE-VERIFIED end-to-end: UPI-SWIGGY txn → reassign+Yes → rule 'swiggy→food_dining' created → sibling UPI-SWIGGY auto-recategorized.

STILL DEFERRED: auto-rule-creation for non-bank-format descriptions (no merchant key); budgets; MF historical-NAV trend wiring. NEXT after PR #5 merges = L3 (MCP Server). Related decision: c679a0f5 (L2 build).
- AI-assisted expense categorization BUILT (feat/ai-assisted-categorization, T1, first LLM integration) — READY TO MERGE — Executed the 18-task TDD plan (docs/superpowers/plans/2026-07-05-ai-assisted-categorization.md; spec 2026-07-05-...-design.md) via subagent-driven-development. Full suite 352 green: core 241 (Groww golden-master 6/6 UNCHANGED), agents 19, api 59, web 33; all tsc --build clean; seam invariant holds (agents no drizzle; core LLM-free — only pre-existing navService NAV fetch). DELIVERED: (1) core new 'keyword' substring rule type, LAST in precedence ladder merchant(exact)>upi_note_keyword(exact)>builtin(substring)>keyword(substring)>null, enforced by CODE ORDER not priority column; keyword rules stored priority 50; CHECK-widening migration drizzle/0002_jazzy_punisher.sql (journal-driven, generated via drizzle-kit; data-preserving table-recreate); new category_source values 'keyword_rule' + 'ai_suggested'. (2) NEW packages/agents package (seeds reserved L4 slot): dialect-shaped LlmProvider interface (LlmDialect gemini|openai-compatible|anthropic), Gemini native adapter (fetch-based, x-goog-api-key HEADER not URL, encodeURIComponent model, maps 429->rate_limit 401/403->auth), openai-compatible + anthropic STUBS (throw provider_not_configured; DeepSeek/OpenAI later = config only), Zod-gated categorizeWithAI (chunk 25 sequential, one retry on bad-JSON OR transient provider throw then skip chunk, drops invalid categoryId, blanks non-substring keyword, auth/provider_not_configured rethrow). (3) api: ExpenseTransactionRepo.listUncategorizedInRange + categorySource in query(); task-keyed llm config from env (GEMINI_API_KEY, AI_CATEGORIZATION_MODEL default gemini-1.5-flash); buildServer injectable llmProvider (mirrors amfiMatch pattern, fake in tests); POST /categories/ai-suggest (month-bounded {from,to}, applies ai_suggested, cap 200, auth->502 provider_not_configured->400); PATCH /transactions/:id/category createRuleKeyword+keyword. (4) web: ExpenseRow.categorySource; useAiSuggest; CategoryChip violet ai_suggested state (confirm=one-off assign->manual, THEN keyword second-prompt gated on LOCAL state so it survives the refetch flip; ✗ reverts to null); ExpensesPage month-scoped 'Suggest with AI' button + review banner + '__ai__' client-side filter. AGGREGATION DECISION (user): ai_suggested counts in spend totals immediately (no summary change), honesty via violet flag. L4 TENSION (user-approved): pulls a thin L4 slice forward (packages/agents) WITHOUT Agent SDK/MCP; anthropic adapter + Agent SDK are L4. DEFERRED (spec §10): AI Settings/usage surface (per-section model routing e.g. categorization=flash, taxation=pro + usage dashboard + key mgmt), additional providers, auto-run-on-import, Confirm-all, persisted usage. FINAL REVIEW found+FIXED 2 blockers: (#1) keyword prompt was erased by confirm's cache-invalidation refetch -> hoisted to local aiConfirmed state; (#2) transient Gemini throw -> 500 -> graceful retry-skip + auth 502 + web surfaces error. GOTCHA: pnpm 11.2.2 AND 11.10.0 crash on Node 20.20.2; only corepack pnpm@10.4.1 works for installs; build/test via node_modules/.bin/{tsc,vitest}. PENDING: expense-redesign PR #6 was merged to main first (month selector dependency); branch rebased onto main. NEXT: push feat/ai-assisted-categorization (gh auth switch --user ak688744) + open PR into main; update MASTER_PLAN.
- AI Settings & Usage feature: brainstorm+plan DONE (T1), per-task provider/model routing + cost tracking + Bedrock; build not started — Brainstorm + plan complete 2026-07-07 (T1, no Groww re-validation — no financial logic touched). Spec docs/superpowers/specs/2026-07-07-ai-settings-usage-design.md; plan docs/superpowers/plans/2026-07-07-ai-settings-usage.md (22 TDD tasks, 5 phases: core→agents→api→web→verify). NO code yet. Docs committed on feat/ai-assisted-categorization (already merged to main via PR #7); build must fetch main + branch feat/ai-settings-usage off it.

FEATURE: user-controlled AI settings surface. Register providers + models (with USER-ENTERED pricing) → route each AI task to a provider+model at runtime from DB → track token+dollar cost per invocation → new two-tab AI web page (Providers & Routing / Usage & Cost). Ships a working Bedrock (Claude via AWS SSO) provider reusing the user's ~/bin/claude_aws login flow.

LOCKED DECISIONS (D1-D10): D1 all config+secrets in DB (not env). D2 secrets AES-256-GCM encrypted at rest, master key from MYFINANCE_SECRET_KEY env (64-hex). D3 polymorphic creds: gemini/openai-compat store encrypted key; BEDROCK stores only {region,profile}, auth via ambient AWS SSO chain (fromSSO), NO secret. D4 code-defined task registry (AI_TASKS) real tasks only; defaultDialect is FIRST-RUN HINT ONLY not a binding. D5 task→provider→model routing fully DB-driven/user-editable. D6 models are first-class entities with user-entered inputPerM/outputPerM; code PRICING_HINTS table demoted to prefill-only. D7 cost frozen at write time. D8 ONE aggregated usage row per task invocation (not per chunk) + call_count. D9 working Bedrock adapter now (JSON via forced tool-use, injectable client for offline tests). D10 two-tab AI page.

ARCHITECTURE: central shift = replace boot-time single-provider injection with an injected LlmGateway (packages/agents/src/gateway.ts) — the choke point. gateway.runTask(task, fn(complete)) resolves route→model→provider from DB, decrypts secret, buildProvider (factory: gemini/bedrock/openai-compat-stub), runs fn accumulating usage across internal complete() calls, writes ONE ai_usage_events row (cost from model's stored price), returns. categorizeWithAI refactored to take injected complete fn instead of raw provider (output identical). 4 new core tables: ai_providers, ai_models, ai_task_routes, ai_usage_events (migration 0004 — 0003 already exists). New crypto.ts in core. Seam invariant holds: agents/core-domain import no Drizzle; gateway takes repo INTERFACES injected by api. buildServer swaps opts.llmProvider for opts.gateway; env llm.categorization path removed (inert).

NEXT: get user's execution choice (subagent-driven recommended vs inline) + user review of plan, then execute via superpowers:subagent-driven-development. GOTCHA carried: pnpm 11.x crashes on Node 20.20.2 → corepack pnpm@10.4.1 for installs, node_modules/.bin/{tsc,vitest} for build/test; tsc --build order core→agents→api→web; push via gh auth switch --user ak688744.
- AI Settings & Usage BUILT (T1, feat/ai-settings-usage) — per-task provider/model routing + cost tracking + working Bedrock; READY TO MERGE — Built 2026-07-09 via subagent-driven-development (22-task plan docs/superpowers/plans/2026-07-07-ai-settings-usage.md; spec 2026-07-07-...-design.md) on branch feat/ai-settings-usage (off main 02e33a7). All 22 tasks done+reviewed. T1 but NO Groww re-validation needed (no financial logic) — gate = Groww golden-master 6/6 UNCHANGED. FULL SUITE 388 green: core 250 + agents 32 + api 69 + web 37; tsc --build clean all pkgs; seam invariant holds (no drizzle/better-sqlite3 in agents/src or core/domain).

DELIVERED:
- core: migration 0004_chief_excalibur.sql (ADDITIVE) adds ai_providers, ai_models (user-entered inputPerM/outputPerM), ai_task_routes, ai_usage_events. crypto.ts AES-256-GCM (encryptSecret/decryptSecret, master key from MYFINANCE_SECRET_KEY 64-hex). 4 repos (aiProviderRepo/aiModelRepo/aiTaskRouteRepo/aiUsageRepo).
- agents: AI_TASKS registry (tasks.ts; defaultDialect is first-run HINT only, not a binding); pricing.ts (PRICING_HINTS prefill + costUsd); bedrock.ts WORKING adapter (@anthropic-ai/bedrock-sdk ^0.32.0 + @aws-sdk/credential-providers ^3.1081.0; AnthropicBedrock({awsRegion, providerChainResolver: async()=>fromSSO({profile})}); JSON via forced tool-use; injectable client for offline tests); factory.ts buildProvider; gateway.ts LlmGateway.runTask — resolves route->model->provider from DB, decrypts, builds provider (INJECTABLE buildProvider dep, not the plan's buildProviderOverride — user-approved), records ONE aggregated ai_usage_events row per invocation (cost frozen from model's stored price over summed tokens; ok=1 success / ok=0 on throw then rethrow; NO row on resolution failure). categorizeWithAI refactored to injected complete fn (output identical).
- api: 4 AI repos + gateway wired into buildServer (opts.gateway replaces old opts.llmProvider; config.llm env path REMOVED/inert). Routes /ai/providers /ai/models /ai/tasks (secret NEVER returned — hasSecret bool; 409 FK guards; 409 on duplicate id; isAiTask validation; price>=0 on POST AND PATCH) + /ai/usage/summary|events. /categories/ai-suggest routes through gateway (auth->502, provider_not_configured->400).
- web: AI sidebar page, 2 tabs. Providers&Routing (polymorphic ProviderFormModal — bedrock hides key field shows region/profile, never sends apiKey; ModelFormModal non-destructive pricing prefill; task routing amber Not-configured chip). Usage&Cost (KPIStat strip, SpendBarChart via toDailyBars, byTask/byModel tables, honest empty state, formatUsd sub-cent-safe). apiClient gained PUT; 6 query + 8 mutation hooks w/ correct invalidation cascades.

FINAL OPUS REVIEW: no CRITICAL; fixed 2 IMPORTANT (I1 PATCH model price validation, I2 duplicate-id->409) + 1 MINOR (M1 web AiUsageEventDTO shape) in commit 79a8202. Security clean: API key encrypted at rest, never in any response/log, transient only in gateway; Bedrock stores no secret.

KNOWN PRE-MERGE MANUAL ITEM (I3): live Bedrock/SSO constructor path is offline-tested only — smoke-test against real `aws sso login --profile dev` per docs/manual_testing/MANUAL_TEST_PLAN.md §B before relying on it. DEFERRED: openai-compatible adapter still a stub (per spec, unlock = config only); persisted-usage pruning; usage budgets/alerts.

NEXT: push feat/ai-settings-usage (gh auth switch --user ak688744) + PR into main; then L3 (MCP Server). GOTCHA: pnpm 11.x crashes Node 20.20.2 — installs via corepack pnpm@10.4.1; build/test node_modules/.bin/{tsc,vitest}, Node 20. Supersedes prior 'build not started' decision 47679e54.
- L3 MCP Server BUILT (packages/mcp): 10 read-only task-shaped tools over core, stdio-via-tsx, PR #12 — READY TO MERGE — L3 (MCP Server) built 2026-07-14 via subagent-driven-development (14-task plan docs/superpowers/plans/2026-07-11-l3-mcp-server.md; spec 2026-07-11-...-design.md). T1 but NO Groww re-validation triggered (no core financial logic touched) — standing gate Groww 6/6 UNCHANGED. Branch layer/3-mcp off main, PR #12 (https://github.com/ak688744/MyFinance/pull/12). Full suite: core 251 + agents 33 + api 69 + mcp 26 green; tsc --build clean across graph; NET diff vs main touches ONLY packages/mcp.

DELIVERED — new packages/mcp: read-only, stdio, DIRECT-TO-CORE MCP server (@modelcontextprotocol/sdk ^1.29.0) exposing 10 task-shaped tools for L4 agents.
- context.ts = single wiring point: buildContext({dbPath?,marketData?}) opens sqlite via core runMigrations, seedDatabase, builds all 13 repos + NavLookup (from core getLatestNAV/getNAVForDate) + INJECTABLE marketData (D7, defaults to real core searchSchemes/getLatestNAV/getNAVHistory) + reserved-UNUSED runInTransaction (D8) + close(). Sqlite handle is a DERIVED TYPE (ReturnType<typeof runMigrations>['sqlite']) — NOT a driver import (seam invariant holds; only a comment mentions better-sqlite3).
- Tool module pattern: each tool file exports pure run<Name>(ctx,input)=>ToolResult handler + register<Name>(server,ctx). shared/output.ts = ok(payload) {content:[{type:'text',text:JSON.stringify}],structuredContent} / errorResult(msg) {isError:true}. server.ts buildServer(ctx) registers all. index.ts stdio entrypoint.
- 10 tools: get_networth_overview, get_investment_portfolio (period+account), get_investment_returns, get_expense_summary, list_transactions, get_loans_overview, get_loan_amortization, list_accounts, search_schemes, get_scheme_nav.
- D5 unit-safety labels: *Inr (raw INR), *Percent (scaled), xirrFraction (RAW fraction, NEVER *100). D6 two-layer validation: SDK Zod inputSchema=protocol layer (malformed→InvalidParams throw); semantic failures (unknown liability id, market outage)→isError; empty data→empty-but-valid.

RUNS VIA tsx: package.json "start":"tsx src/index.ts", main/bin→src/index.ts, extensionless relative imports. The monorepo is NEVER compiled to JS — it runs TypeScript directly via tsx (the API uses "tsx src/server.ts"). tsx does NOT block a future HTTP transport (transport is a separate axis; buildServer is transport-agnostic — swap StdioServerTransport for HTTP in index.ts, no tool changes).

KEY DEVIATIONS FROM PLAN (all correct vs real core): (1) getNAVHistory is 3-arg (amfiCode,startDate,endDate) not 1-arg; MarketData type + get_scheme_nav history path use 3-arg (2000-01-01..today). (2) SchemeInfo already has amfiCode (identity map, NOT schemeCode as plan assumed — plan's mapping would've yielded undefined). (3) expense excludeFromSpend=['investment','self_transfer'] to match CURRENT API default (plan's 'transfer' was stale). (4) list_transactions.count=page size (repo query() has no match-total). (5) assetValuationRepo uses insert() not create().

FINAL OPUS REVIEW found 2 Important bugs in investments.ts, both FIXED via TDD w/ regression tests (commit b3cb98f): I1 account filter was dropped from getPortfolioSummary (account-scoped call returned whole-portfolio summary); I2 period param advertised-but-ignored → wired through core getPortfolioSummaryForPeriod/getHoldingsForPeriod (user chose spec-faithful; allocation stays lifetime — no ForPeriod variant in core). Accepted minors: get_scheme_nav.latest.dateStr null (getLatestNAV number-only); tsconfig outDir load-bearing for composite.

INCIDENT (resolved): Task-13 entrypoint subagent tried to make it runnable by COMPILING core to dist JS — rewrote imports across 37 core files + changed core migrationsFolder ../../drizzle→../../../drizzle → broke 151 core tests incl Groww. Root cause: monorepo never compiles. Reverted ALL core (byte-identical) + switched to tsx. LESSON: dispatch prompts for "make X runnable" tasks must state the run-via-tsx convention explicitly to prevent compile-to-dist drift.

DEFERRED (spec §8): write tools (tools/write/ reserved+empty, runInTransaction built-unused — L4 behind opt-in); HTTP transport; extra tools (categories/rules/import-history/budgets). NEXT = merge PR #12, then L4 (AI/Wealth Manager) consuming this tool contract via Claude Agent SDK.

GOTCHA: corepack prepare pnpm@10.4.1 now FAILS (resolves pinned 11.2.2 which crashes Node 20.20.2). WORKING install: node ~/.cache/node/corepack/v1/pnpm/10.4.1/dist/pnpm.cjs install --config.manage-package-manager-versions=false. Build/test via node_modules/.bin/{tsc,vitest}; mcp test bin at packages/mcp/node_modules/.bin/vitest; tsx at packages/mcp/node_modules/.bin/tsx.
- L4.0 Wealth Agent: framework + scope decisions locked in brainstorm
- L4.0 wealth-agent-harness TDD plan complete (Mastra); Node 22 bump + fastembed + Bedrock-agent deferred — L4.0 plan written 2026-07-18 (T1, superpowers:writing-plans) at docs/superpowers/plans/2026-07-18-l4.0-wealth-agent-harness.md over spec 2026-07-18-l4.0-wealth-agent-harness-design.md. 15 tasks (0-14), TDD, on branch layer/4.0-wealth-agent-harness. NO code yet.

KEY PLANNING DECISIONS (user-approved via AskUserQuestion this session):
1. NODE 22 BUMP — spec §11 wrongly said "Mastra floor Node 18+ compatible with our Node 20". Verified @mastra/core 1.51 + ai v7 require node>=22.13. Plan Task 0 bumps .nvmrc 20→22 + root engines, re-verifies whole monorepo under Node 22 FIRST. All commands now `nvm use 22`. This interacts with the pnpm gotcha (11.x crashed Node 20.20.2) — Task 0 re-establishes the working pnpm command under Node 22.
2. EMBEDDER = fastembed (@mastra/fastembed, local ONNX, no API key) for semantic recall — matches local-first/model-independence. Over OpenAI text-embedding-3-small.
3. BEDROCK-FOR-AGENT DEFERRED to L4.1 — v1 wealth agent supports gemini + openai-compatible dialects only (per-turn AWS profile with Mastra unverified). modelResolver throws AgentConfigError('unsupported_dialect') for a bedrock wealth_chat route. Categorization's working Bedrock path untouched.

CONFIRMED MASTRA API (from parallel research subagents, saved as research findings this session, tags mastra/l4): @mastra/core 1.51.0, @mastra/memory 1.23, @mastra/libsql 1.16 (LibSQLStore+LibSQLVector same pkg), @mastra/mcp 1.14 (MCPClient class; listTools()/listToolsets(); stdio via {command:'tsx',args:[entry],env}; tools namespaced <serverName>_<tool> NOT mcp__), @mastra/fastembed. Agent from @mastra/core/agent; model as 'provider/model' string OR object {id,apiKey} (literal per-turn key — CRITICAL, no process.env mutation) OR resolver fn ({requestContext})=>model. stream() returns MastraModelOutput with .textStream + await .usage ({inputTokens,outputTokens,totalTokens}). Standalone Agent works — no Mastra container needed. Mock model = MockLanguageModelV4 from 'ai/test' + simulateReadableStream from 'ai'. PLAN TASK 2 IS A CAPABILITY SPIKE that pins the 2 doc-unconfirmed shapes (mock tool-call content-part; usage field names) before building — do not assume, the spike teaches the real API.

LOCAL SEAMS (research saved): AI_TASKS in packages/agents/src/tasks.ts (add wealth_chat); makeLlmGateway (factory not class) route→model→provider→decrypt→one ai_usage_events row/runTask — mirrored in new agentic runChat but Mastra owns its loop; core crypto at packages/core/src/db/crypto.ts (encryptSecret/decryptSecret, MYFINANCE_SECRET_KEY 64-hex); AI repos aiTaskRouteRepo.getByTask/aiModelRepo.get/aiProviderRepo.get/aiUsageRepo.insert(Omit<row,'id'>); costUsd(in,out,inPerM,outPerM) in pricing.ts; MCP buildContext({dbPath,marketData}) accepts :memory: + 32 tools (10 read+22 write, delete_*/recategorize_all preview-gated); api buildServer(BuildServerOpts) Fastify plugins app.repos, gateway via plugins/gateway.ts makeGateway(repos), NO existing SSE (agent route is first, use reply.hijack()+reply.raw); web Assistant placeholder at apps/web/src/features/assistant/AssistantPage.tsx, NO existing stream consumer (apiStream is first, fetch().body.getReader()+TextDecoder); tests vitest test/**/*.test.ts, api uses buildServer({dbPath:':memory:'})+app.inject, agents hand-written fake repos.

NEXT: execute via superpowers:subagent-driven-development (recommended). Package structure = new packages/agent-harness (@myfinance/agent-harness): modelResolver/mcpClient/memory/wealthAgent/runChat/index. Then api plugins/harness.ts + routes/agent.ts (POST /agent/chat SSE) + server.ts wire. Then web lib/apiStream.ts + features/assistant/useAgentChat.ts + AssistantPage.tsx. Gates: Groww 6/6 unchanged (zero core change), seam invariant (no drizzle/better-sqlite3 in agent-harness/src), full suite green Node 22.
- L4.0 Wealth Agent Harness built with Mastra on Node 22: packages/agent-harness (per-turn wealth_chat route, MCP stdio tools, LibSQL memory+fastembed), POST /agent/chat SSE, web Assistant chat; Bedrock-for-agent deferred to L4.1. — Executed the 15-task TDD plan; Groww 6/6 unchanged; full suite green under Node 22 after better-sqlite3 native rebuild.
- undefined
- Wealth agent chat UX #1 (concise/step-collapse) + #2 (markdown rendering) shipped (T2) — 2026-07-30, T2, apps/web + packages/agent-harness + packages/api, no core/Groww. Grounded in reading the user's real last thread (thread-20260730185907887-5) which showed: step-narration text ('Let me pull...','Perfect!') + giant 4-table answers + raw markdown pipes (no FE renderer). FIXES: (#1 verbosity) tightened WEALTH_INSTRUCTIONS in wealthAgent.ts to answer-first, work-quietly-no-narration, show-key-reasoning-precisely-non-repetitive, match-depth-to-question, GFM formatting. PLUS step-collapse: runChat.ts now drains stream.fullStream (not textStream) via NEW pure streamEvents.ts mapChunk(chunk)->{type:'text'|'step',...} (text-delta->text; tool-call->step w/ toFriendlyToolLabel; else null). ChatResult gains events:AsyncIterable<HarnessEvent> (text-only textStream kept as derived view for back-comaptibility; falls back to textStream when mock has no fullStream). api routes/agent.ts emits new SSE {type:'step',label} alongside token. web: apiStream AgentEvent += step; useAgentChat collects step labels into ChatMessage.steps[]; NEW StepsTrail.tsx (collapsed 'N steps' expander; live 'label...' status while streaming w/ no answer yet). (#2 markdown) NEW Markdown.tsx = react-markdown@9 + remark-gfm@4 (added to apps/web) w/ tailwind-styled table/heading/list/code components; assistant bubbles render Markdown (user + error bubbles stay plain). LIVE-VERIFIED vs real demo.db+haiku: 'spend last month' answer went from multi-thousand-char/4-table to 710 chars answer-first + one table + precise 'largest chunks ~77%' line; 3 tool calls collapsed to steps trail. KNOWN minor (user ACCEPTED, won't fix): text emitted right before a tool-call in the FINAL step leaks 2 short narration lines into the answer (mapper treats all text-delta as answer; would need stateful buffer-until-tool-call boundary - user said 'I dont mind this small leak'). GATES: agent-harness 32 (+streamEvents 8), api 80, web 58; all tsc --build clean. #3 (interactive clarifying questions w/ options) = separate T1 brainstorm, NOT started. Prior context: bedrock-for-agent (d1c18264), empty-response fix + error bubble + localStorage persistence (f3a1b425), Mastra fullStream shapes research.
- Wealth agent response-calibration + interactive-questions: SPEC DONE, plan+build NEXT SESSION (T1, branch feat/agent-response-calibration) — Brainstorm complete 2026-08-01 (T1, no core/Groww). Spec committed: docs/superpowers/specs/2026-08-01-wealth-agent-response-calibration-interactive-questions-design.md (commit 4ce85f0) on branch feat/agent-response-calibration (off main). NO implementation code yet for these two features. NEXT SESSION: invoke superpowers:writing-plans over the spec -> TDD plan -> execute via subagent-driven-development on the SAME branch.

WHY: analyzed user's real last thread thread-20260730185907887-5 (in demo.db.memory.db, mastra_messages) — EVERY assistant msg was 14k-19k chars (emoji-header mega-reports, 6+ tables, multi-phase roadmaps) regardless of question focus. Prior shipped work (markdown/step-collapse/answer-first instructions) fixed FOCUSED questions (spend-last-month 19k->710 chars) but did NOT hold for OPEN-ENDED advisory questions ('what do you suggest for extra investments' etc). 

TWO FEATURES IN THE SPEC (both user-approved via AskUserQuestion):
1. VERBOSITY CALIBRATION (prompt-only): rewrite WEALTH_INSTRUCTIONS in packages/agent-harness/src/wealthAgent.ts with explicit calibration rules (quick Q->1-3 sentences; analysis Q->conclusion-first focused, <=1 table unless full breakdown asked) + anti-pattern list + 2-3 FEW-SHOT example exchanges (the key lever — plain rules didn't hold; user wants 'like Claude Code' judgment, NOT a rigid cap or a verbosity toggle — both rejected). Not unit-testable; verify by re-running real demo.db queries + comparing sizes.
2. INTERACTIVE CLARIFYING QUESTIONS (ask_user tool, ASK-AND-END-TURN — suspend/resume REJECTED): new LOCAL Mastra tool ask_user({question, options:[{label}]}) via createTool in agent-harness (NOT in packages/mcp — it's conversation-control, no finance backend; merge with MCP toolset in runChat). Single-select chips + free-text ALWAYS allowed (not a per-call flag). New SSE event {type:'question',question,options}. streamEvents.mapChunk maps tool-call w/ toolName==='ask_user' -> question event (NOT step); other tool-calls still -> step. runChat surfaces it + ends turn. routes/agent.ts emits SSE frame. web apiStream AgentEvent+=question; useAgentChat attaches {question,options} to assistant msg; NEW QuestionChips.tsx; AssistantPage renders chips, input stays active; click chip -> send(label). Mastra memory carries context to next turn (seamless). Malformed ask_user (no options) -> question event w/ options:[] (free-text only); unparseable -> ignore.

COMPONENTS (§4 of spec): wealthAgent.ts (instructions), NEW askUserTool.ts, streamEvents.ts (+question mapping +HarnessEvent variant), runChat.ts (merge tool, pass question events), index.ts exports; api routes/agent.ts; web apiStream.ts, useAgentChat.ts (ChatMessage.question?), NEW QuestionChips.tsx, AssistantPage.tsx. NO core/mcp change. Tier T1 (new agent capability + new stream contract) but zero financial logic -> Groww untouched.

ALSO DONE THIS SESSION (committed 6b21bb6 on same branch, ONE commit, all green agent-harness 32/api 80/web 58): the PRIOR 3 features — (a) Bedrock dialect for wealth agent (modelResolver + new agentModel.ts, @ai-sdk/amazon-bedrock@^4 + @aws-sdk/credential-providers deps, per-turn fromSSO, spike-verified live; NOTE sonnet-5 profile is intermittently 500/503 — route to opus-4-8/sonnet-4-6/haiku-4-5 which tested reliable), (b) error-bubble-inline + localStorage chat persistence + New chat (useAgentChat/AssistantPage), (c) markdown rendering (Markdown.tsx react-markdown@9+remark-gfm@4) + step-collapse (streamEvents.mapChunk over fullStream, SSE step event, StepsTrail.tsx). These are NOT yet PR'd/merged. Accepted trade-off: small pre-tool narration leak ('Let me analyse...') left as-is per user.

GOTCHAS: Node 22 (nvm use 22); pnpm via node ~/.cache/node/corepack/v1/pnpm/10.4.1/dist/pnpm.cjs ... --config.manage-package-manager-versions=false; build/test node_modules/.bin/{tsc,vitest}; run via tsx (never compile); real app DB demo.db + demo.db.memory.db (Mastra LibSQL, read via sqlite3 CLI not better-sqlite3); push via gh auth switch --user ak688744. Related decisions: d1c18264 (bedrock), f3a1b425 (empty-response/persistence), chat-UX #1#2 decision this session.
- Feature B (Expense AI Insights + Notes→Tags + Expense Clarity Agent) BUILT — T1, branch feat/agent-response-calibration, READY FOR PR — Executed the 24-task TDD plan (docs/superpowers/plans/2026-08-05-expense-ai-insights-tags.md; spec 2026-08-05-...-design.md) via superpowers:subagent-driven-development on branch feat/agent-response-calibration (Feature-B commits ea434d0..5619e8b; diff base b02051e). Supersedes prior 'SPEC+PLAN done, no code' decision 29b52df7. T1 but NO Groww re-validation triggered — categorize.ts BYTE-IDENTICAL, Groww golden-master 6/6 unchanged. FULL SUITE 583 GREEN: core 268 + agents 37 + api 87 + mcp 74 + agent-harness 50 + web 67. tsc --build clean beyond 2 PRE-EXISTING baseline errors (packages/core/src/db/migrate.ts:12 URL-type Node22 @types drift, byte-identical to base; apps/web/test/useAgentChat.test.ts:121 AsyncGenerator arg-count — both prior-branch, unrelated to Feature B). Seam invariant clean (no drizzle/better-sqlite3 in core/domain, agent-harness/src, mcp/src).

DELIVERED —
(A) TAGS: tags TEXT JSON column [{tag,source:'user'|'agent'}] on transactions (migration 0007_transaction_tags.sql, hand-written ALTER + journal idx 7, NO junction table; note column stays). Pure packages/core/src/domain/tags.ts (normalizeTags lowercase/trim/dedupe last-source-wins, parseTags/serializeTags null-when-empty, mergeTags/removeTagFrom). ExpenseTransactionRepo.get/set/add/removeTag + tags parsed on query() rows. API PATCH /transactions/:id/tags {tags,mode:add|replace} + DELETE /transactions/:id/tags/:tag (source='user'). MCP tag_transaction write tool (source='agent', additive/not-preview-gated; write tools 22→23). Web TagChips per row (agent=violet, user=gray direct Tailwind matching CategoryChip ai_suggested, not the brief's semantic ai/ tokens).
(B) INSIGHTS: pure packages/core/src/domain/insights/expenseInsights.ts, 3 deterministic detectors w/ named-const thresholds (needs_clarity=debit∧untagged∧(categoryId null ∨ deriveMerchantName→null), ONE grouped card/month id needs-clarity:<month>; new_spend=merchant not in prior LOOKBACK_MONTHS=3, total≥NEW_SPEND_MIN_INR=500, sorted desc top NEW_SPEND_TOP_N=5; abnormal_spend=ratio>ABNORMAL_RATIO=1.4 AND jump≥ABNORMAL_MIN_JUMP_INR=1000, severity warn). deriveMerchantName INJECTED (kept out of core; ported to packages/api/src/lib/deriveMerchantName.ts from apps/web format.ts). GET /expenses/insights?month=YYYY-MM → {data:Insight[]}, NO LLM, 400 on bad/missing month; assembles InsightInput from expenseTxRepo.query+summary over month + 3 prior-month windows. Web insight cards + useInsightDismissal (localStorage key myfinance.expense.insights.dismissed.v1) + useExpenseInsights(month) query key ['expenseInsights',month]. Self-heals: tag/categorize/txn mutations invalidate ['expenseInsights'].
(C) EXPENSE CLARITY AGENT: buildExpenseAgent in packages/agent-harness (ISOLATED memory resource 'expense-agent' — no wealth-recall pollution; focused EXPENSE_TOOL_ALLOWLIST = finance_list_transactions/get_expense_summary/list_categories/categorize_transaction/tag_transaction/create_category/create_rule + ask_user, NO investment/loan/networth; filterTools pure Set filter). NEW list_categories MCP read tool (read tools 10→11; agent needs category ids). OWN expense_agent AI task in packages/agents/tasks.ts. resolveRoute(deps,task) generalizes resolveWealthRoute (kept as wrapper). runChat({agent:'wealth'|'expense'}) switches task/builder/tools/memory-resource; usage row records resolved task. Extracted reusable ChatPanel({chat,emptyState?,placeholder?}) from AssistantPage. ExpensesInsightDrawer (right slide-over, useAgentChat({agent:'expense',persist:false,storageKey}), seeds via pure buildInsightSeed once w/ useRef guard, onClose invalidates expenses+expenseInsights). agent field threads web apiStream→POST /agent/chat→runChat. useAgentChat gained {storageKey,persist,agent} — persist:false gates BOTH load and save (no localStorage leak).
CATEGORIZATION CHANGE (intentional, non-frozen packages/agents): categorizeWithAI gained minConfidence (default 0.9) → drop <0.9 (leave blank, count skipped). /categories/ai-suggest now leaves low-confidence uncategorized (feeds needs_clarity) + on confident apply creates a merchant rule via extractMerchantKey+saveCategoryMemoryRule (try/catch swallows ONLY unique-collision), does NOT recategorize.

FINAL OPUS REVIEW: no Critical. 2 Important FIXED (commit 5619e8b, re-review all ADDRESSED): (I1) useUpdateTxCategory/useAiSuggest/useUpdate|Delete|CreateTransaction now also invalidate ['expenseInsights'] so needs_clarity self-heals on categorize (spec §4.6); (I2) rowsForIds used paginated txns.data (PAGE_SIZE=25) but insight.transactionIds span whole month → added unpaginated fullMonthTxns query (limit 1000, month-scoped) for the drawer seed, paginated table query unchanged. Removed dead priorEnd var. DEFERRED minors: abnormal_spend card shows raw categoryId not friendly name (cosmetic, live-confirmed); ChatMessage type duplicated ChatPanel.tsx vs useAgentChat.ts.

LIVE-VERIFIED (deterministic no-LLM path, real demo.db via buildServer+inject): GET /expenses/insights 2026-06 → 8 insights (needs_clarity 3, 5×new_spend, 2×abnormal_spend), 2026-05 → 11 insights (health up 3213% etc), no-month → 400. REMAINING manual (needs live AI route + browser): full round trip card→drawer→expense-agent ask_user→tag_transaction chip; Suggest-with-AI leave-blank→needs_clarity; wealth Assistant regression after ChatPanel extraction.

GOTCHAS confirmed: Edit tool repeatedly converted ASCII quotes→curly Unicode in agent-harness/web files breaking esbuild — implementers worked around via bash heredocs; verified none leaked (grep -P smart-quotes clean). Node 22 (nvm use 22); NEVER pnpm install; build/test node_modules/.bin/{tsc,vitest}; tsx never compile. NEXT: push feat/agent-response-calibration + ONE PR into main covering calibration + Feature B (gh auth switch --user ak688744), then L4.1 orchestrator+specialists (Expense Clarity Agent is its early slice).
- LLM triage layer over rule-based expense insights (recall→precision) BUILT — T2, branch feat/agent-response-calibration — Built 2026-08-07 (T2, user chose T2 over my T1 rec — no frozen financial logic, Groww untouched). Solves: rule-only insights raised FALSE ALARMS (e.g. rent flagged new_spend every month because payee/description varies, though category already made it clear). Solution = keep cheap deterministic rules as RECALL layer, add LLM as PRECISION filter + question-framer. Spike-validated first (throwaway packages/agent-harness/spike-insights-triage.mts, now gitignored) on real demo.db June+April: LLM killed the rent false alarm, caught a real rent/loan MISCATEGORIZATION bug, decomposed the ₹2L car-purchase spike, and produced forecasting-ready tags. Two spike iterations taught the two production must-haves: (1) CONSOLIDATION must be SEMANTIC not naive union-find (broad detectors needs_clarity/abnormal_spend would act as universal connectors and collapse everything into one giant card); (2) explicit 3-TIER suppress/needs_input/worth_knowing rule in the prompt (model judgment was good but uneven without it).

NORTH STAR (user reframed): the Expense Clarity Agent's job is to ensure every transaction carries category+tags so future BUDGETING/FORECASTING understands spend. Insights = the data-completeness WORK QUEUE driving transactions to metadata-completeness, NOT an interestingness feed. Suppress rule = 'suppress when forecasting-complete (has cadence tag OR self-explanatory category like rent/EMI/SIP)'. Forecasting itself DEFERRED (later T1) — this build only makes data forecasting-ready.

TAG MODEL (user): each kept event's options carry BOTH a controlled cadence tag (recurring|one_off|subscription|reimbursable|irregular — drives forecast math) AND 0..N free descriptive snake_case tags (car_purchase, home_setup, trip — give agent the WHY). E.g. car → [one_off, car_purchase].

ARCHITECTURE (Approach C — persist, chosen over A because per-refresh LLM calls + non-deterministic reshuffle = bad UX):
- core/domain/insights/consolidateInsights.ts: new_spend merged BY MERCHANT; needs_clarity+abnormal_spend each own event. Emits InsightEvent[] each with a stable FNV-1a `signature` = grouped txnIds + their category + sorted tags. Answering a Q mutates a txn → signature changes → only that event re-triages; untouched events reuse cached verdict verbatim (PER-EVENT cache, no reshuffle of stable cards). migration 0008_expense_insight_triage (table keyed by signature, verdictJson) + makeExpenseInsightTriageRepo (getMany/upsert onConflictDoUpdate).
- agents/src/insights/{schema,prompt,triageInsights}.ts: 3-tier prompt (ported from spike), Zod-gated, options:[{label,tags,categoryFix}] so tapping a button applies THAT option's metadata with NO further LLM call (different options → different tags). One gateway.runTask('expense_insight_triage') call → 1 ai_usage_events row. Retry-once-on-bad-JSON then FALLBACK to keep-as-needs_input (never silently drop an event). Rethrows auth/provider_not_configured. New AI task 'expense_insight_triage' in tasks.ts.
- api/src/lib/insightsService.ts: buildMonthEvents (rules→consolidate) + getTriagedInsights (cache lookup by signature → triage only stale events → persist → assemble, drop suppressed, attach txnIds). GRACEFUL: if no gateway/route or triage throws, serves untriagedCard() (raw rule as needs_input) so page never blank without AI. GET /expenses/insights now returns triaged cards (shape CHANGED: type→tier, title→refinedTitle, transactionIds→txnIds, +options/question/reason). NEW POST /expenses/insights/resolve {transactionIds, tags?, categoryFix?} → addTags(source='agent') + updateCategory(source='manual') → self-heals. expenseRoutes now takes {gateway}; wired in server.ts. New repo threaded through plugins/db.ts Repos.
- web: types.ts Insight reshaped; useResolveInsight hook; InsightCards.tsx rebuilt = two lanes (Needs input / Worth knowing), inline option buttons + free-text box + Discuss link. INTERACTION (user decided, Option B): inline single-shot resolve (tap option = instant apply; free-text → opens Discuss drawer seeded with the text for the expense agent to interpret — the messy ~10%); drawer opens ONLY on explicit click, never auto-escalates (honors user's 'no UI moves on its own' principle). ExpensesInsightDrawer + insightSeed take seedText.

GATES: full suite 608 green (core 279 + agents 43 + api 92 + mcp 74 + agent-harness 50 + web 70); tsc --build clean all pkgs; Groww golden-master 6/6 UNCHANGED. LIVE-VERIFIED end-to-end through real endpoint on demo.db April via Bedrock haiku: car→one-off-vs-loan options with categoryFix, rent/loan miscat own card, shopping-spike→worth_knowing (no Q), cold 20.7s (1 LLM call) / warm 7ms cached / titles identical (stability holds). Had to add expense_insight_triage route to demo.db (→haiku-4.5) for the live test.

DEFERRED: forecasting (own T1, consumes these tags); free-text inline interpretation currently routes to Discuss drawer rather than a dedicated interpret endpoint (v1 simplification). NEXT: this stacks on the Feature-B/calibration branch feat/agent-response-calibration; will go in the same eventual PR. Node 22; node_modules/.bin/{tsc,vitest}; tsx never compile; push gh auth switch --user ak688744.
- needs_clarity insights: hybrid — deterministic floor + self-explanatory-category exclusion, and one event PER transaction (not a grouped card) — BUILT + live-verified 2026-09-05 (T2, user chose hybrid). Branch feat/credit-card-bill-split (worktree). Solves: the single grouped needs_clarity card kept asking for cadence on self-explanatory/immaterial txns (₹3.45L salary, ₹60k rent, two ₹52 dividends, ₹23 forex markup) because the detector flagged ANY untagged txn where deriveMerchantName→null even when already categorized, and because the LLM triage suppress-tier is ALL-OR-NOTHING per event (one grouped card can't drop salary while keeping unknowns).

CHANGE (packages/core/src/domain/insights/expenseInsights.ts, computeExpenseInsights needs_clarity block):
1. New consts: NEEDS_CLARITY_MIN_INR=100 (amount floor — skip immaterial dividends/fees/markup); SELF_EXPLANATORY_CADENCE_CATEGORIES=Set['salary','rent','loan'] (assumable recurring → not flagged; 'investment' deliberately ABSENT so SIP-vs-lumpsum still asked).
2. New flagged filter: skip if tagged; skip if amount<floor; flag if categoryId===null (needs a category); skip if categoryId in self-explanatory set; else flag iff deriveMerchantName===null (ambiguous cadence). 
3. Emit ONE Insight PER remaining txn (id `needs-clarity:<month>:<txnId>`, transactionIds:[t.id]) instead of one grouped card. consolidateInsights already turns each needs_clarity candidate into its own event (event:<id>) so per-item events + per-item triage fall out automatically; the LLM suppress/needs_input tier now acts per-item. Web InsightCards keys only on tier → renders multiple cards fine (no UI change needed).

LIVE (demo.db Aug, cold triage after clearing stale cache): went from 6-item grouped 'need cadence tags' card → 2 cards: `event:needs-clarity:2026-08:978` "₹15,000 investment — cadence unknown" (genuinely ambiguous) + `event:new_spend:THIRUPATHI` (real new merchant). Salary/rent/dividends/markup all correctly gone.

KNOWN MINOR (pre-existing, surfaced here): consolidateInsights.signatureFor = hash(sorted txnIds + each txn's category + sorted tags) and does NOT include event type/eventId. So a per-item needs_clarity event and an old new_spend event for the SAME single txn collide on signature → the cache serves whichever verdict was stored (stale eventId/title). Manifested as the Aug investment card briefly showing the old "new payee" new_spend title until the stale row was cleared. Benign going forward because after the merchant-key ref-number fix (b5bc154d) new_spend no longer fires for recurring items, so a txn is usually flagged by only one detector; legacy pre-fix cache rows just need clearing (self-heal is by signature match). If cross-type collisions ever bite, add event type to signatureFor (mass re-triage cost). NOT done now (scope).

TESTS: expenseInsights.test.ts updated (grouped→per-item) + 2 new (floor/self-explanatory suppression, per-item ids). Core 289 + api 107 green, Groww 6/6 unchanged. NOT committed. Related: merchant-key ref fix b5bc154d, mechanism finding 0b91cf7d.

## Convention
- Tiered SDLC for agent-built work: T1 full discipline / T2 light / T3 direct, with core-logic override and SessionStart-hook triage — Work is triaged by size to save tokens. T1 (layer/substantial feature OR any core-logic change): full discipline — brainstorm spec → writing-plans → branch → TDD → subagent review → PR → update master plan. T2 (scoped, few files): short inline plan → implement → tests → self-review. T3 (localized bug/tweak): direct fix → verify. OVERRIDE: any change touching core financial logic (XIRR, categorization, parsers, portfolio math) is ALWAYS T1 with Groww re-validation, regardless of size (it regresses silently). When unsure, round up a tier. Enforced by a minimal SessionStart hook that injects 'classify tier per myfinance-sdlc skill'; the fat procedure lives in .claude/skills/myfinance-sdlc and lazy-loads. CLAUDE.md holds a backstop pointer. Plugin rejected (YAGNI). MASTER_PLAN.md is durable state, read-first/update-last every session.
- Ask before spending effort on low-value fixes that require disproportionate change — User feedback 2026-07-30: for small/unimportant problems that turn out to require many changes, STOP and ask first with a quick cost/benefit rather than diving in. Don't waste time perfecting things that don't matter. WHY: the narration-leak in the wealth-chat step-collapse would have needed a stateful buffer-until-tool-call mapper refactor for a 2-line cosmetic gain; user said 'I dont mind this small leak' and then 'ask me before itself'. HOW TO APPLY: when a fix's change-size grows beyond its value, pause and offer the tradeoff (fix vs accept vs defer) before implementing. Bias toward shipping the high-value 80% and asking about the long-tail 20%.

## Testing
- L0 core package COMPLETE — Groww golden-master passed (max 0.046pp), Phase 6 API remains — L0 progress checkpoint 2026-06-12. Branch layer/0-foundation, 25 commits past af19bd2. packages/core is COMPLETE: schema+migrations, 7 repos, full domain port (xirr verbatim, returns/portfolio repo-injected, categorize, nav, amfi), import parsers+orchestration, seed + public index.ts. Tests 170/170 green.

T1 CORE-LOGIC GATE PASSED: Groww golden-master (packages/core/test/golden/groww.golden.test.ts, fixture growwGolden.json — anonymized, PII-stripped, committed) validates ported XIRR per-scheme vs Groww's real reported XIRR across 6 schemes / 104 real SIP transactions. Max abs diff 0.046pp, all within 0.5pp tolerance. SCHEME_D matched to 0.001pp. The port is provably faithful to production financial truth. Characterization tests (4.1) also pin the pipeline.

SEAM INVARIANT VERIFIED: no drizzle/better-sqlite3 import anywhere in domain/ or import/ (only architecture comments mention them). Pure logic depends only on repo interfaces.

REMAINING: Phase 6 (Task 6.1 Fastify skeleton+config+errors, 6.2 db plugin+/health, 6.3 read endpoints /transactions + /investments/summary + /investments/returns) then Phase 7 close-out (full verify, subagent final review, PR, update MASTER_PLAN, memory). Spec docs/superpowers/specs/2026-06-12-l0-foundation-design.md; plan docs/superpowers/plans/2026-06-12-l0-foundation.md. Node 20 required for all commands.
- AI suggest tests migrated to gateway-based architecture — Task 17: Removed obsolete config.test.ts llm block (ApiConfig.llm was deleted in Task 14). Updated aiSuggest.test.ts to use gateway instead of llmProvider: makeFakeGateway wraps provider+optional usageRepo for recording; makeErrorGateway throws LlmError. Added e2e test proving ai-suggest records exactly 1 usage row via fake gateway that manually inserts into aiUsageRepo. All 67 api tests green, tsc --build clean.

## Scope
- Insert L1.5 Unified Investment Model (all asset classes) before L2; build L1 on current MF model first with multi-platform + multi-account seams — User wants the investment section to be a single net-worth view across ALL asset classes (mutual funds, stocks, PPF, FD, cash), each with its own ingestion method (some manual, some file-import), plus multiple accounts per asset class. Decided 2026-06-13:

SEQUENCING: Build L1 (Ingestion + Data API) NOW on the current MF-only model, then insert a NEW layer L1.5 "Unified Investment Model" before L2 (Web UI). Rejected "redesign foundation first" — L1's ingestion PATTERN (multipart HTTP layer + parser registry + normalized ParsedData + transactional writes) survives the generalization, so L1 is proving ground not throwaway, and it keeps a working Groww-validated vertical slice at every step.

L1 SEAMS (baked in now): (1) multi-platform parser registry keyed by `platform` form field — registry maps (platform, kind) -> parser emitting the SAME normalized ParsedData the orchestrators consume; Groww + HDFC initially, ET Money etc. are drop-in modules with zero endpoint/orchestrator change. (2) multi-account first-class — schema already has accountName + investmentApp on every investment table + core getAccounts()/account filter; L1 adds GET /investments/accounts. Multiple Groww MF accounts = different accountName; Groww vs ET Money = different investmentApp.

L1.5 MODEL: asset classes differ on TWO axes — INGESTION (file-import via L1 registry vs manual-entry CRUD; PPF/FD/cash are user-entered) and VALUATION (market = qty x live price API; computed = interest compounding to date for PPF/FD; manual = user-stated). Generalized schema ~= accounts + assets(assetClass) + generalized cashflows + per-class valuation/ingestion strategy + net-worth rollup. Reshapes core schema => T1 with Groww re-validation; migrate existing MF data in, golden-master is safety net. Only investment orchestrators/repos generalize; expense import + L1 parser/HTTP layer carry forward.

ALSO IN L1.5 SCOPE (added 2026-06-13): EXPENSE multi-account. Current expense model (transactions table) has NO account dimension — single implicit account. L1.5 should generalize the `accounts` concept across BOTH expenses and investments so the user can have multiple bank/expense accounts too, not just multiple investment accounts.
- L1 Ingestion spec + plan complete and committed; build not started — resume by executing the TDD plan — L1 (Ingestion + Data API) T1 brainstorm + planning phase DONE 2026-06-13, on main (docs-only commits 1538739, 5493ef5, 98af099, ad60b75, 916d619). NO code written yet — only docs. Resume in a new session by executing the plan.

SPEC: docs/superpowers/specs/2026-06-12-l1-ingestion-design.md. PLAN: docs/superpowers/plans/2026-06-13-l1-ingestion.md (10 TDD tasks, fully self-contained — exact file paths, code, commands, expected output). MASTER_PLAN L1 row links both + §8 has a "RESUME HERE" pointer.

WHAT L1 BUILDS (packages/api HTTP layer over core, no new package): 3 multipart import endpoints (POST /imports/expenses, /imports/investments/holdings, /imports/investments/transactions) behind a multi-platform PARSER REGISTRY (src/import/registry.ts, resolveParser(platform,kind); hdfc+groww now); GET /imports; expanded reads GET /investments/holdings|allocation|accounts (multi-account D7); categories GET + rule CRUD (POST/PATCH/DELETE /categories/rules) + POST /recategorize. Seams: txRunner (sqlite.transaction), injectable amfiMatch via buildServer({amfiMatch}) for offline tests, reuse L0 NavLookup. unmatched_schemes returned as 200 payload (D2). One non-financial core add: ImportHistoryRepo.listAll().

EXECUTION: use superpowers:subagent-driven-development, fresh subagent per task, start Task 0 (branch layer/1-ingestion off main + add @fastify/multipart@^9). GOTCHAS baked into plan: Node 20 mandatory (source ~/.nvm/nvm.sh && nvm use 20); typecheck via `tsc --build` not --noEmit (api->core project ref); fixtures generated programmatically with xlsx (test/fixtures/makeFixtures.ts) to match parser header-alias detection, PII-free, with a parse-sanity gate before building routes; @fastify/multipart NOT yet installed; push/PR needs `gh auth switch --user ak688744`.

NEXT AFTER L1: L1.5 Unified Investment Model (see decision 437133fd).
- NEXT SESSION: run a standalone frontend/platform DESIGN SPIKE (visual mockups) before L1.5 — feeds L1.5 + L2 specs — Decided 2026-06-13 (right after L1 completed, PR #2 open). Before building L1.5, do ONE round of frontend/platform design as a STANDALONE design spike — NOT a build. Rationale: L1.5 is the expensive layer (reshapes core schema to all asset classes + first-class accounts, T1 WITH Groww re-validation); the best input to that schema is what the UI needs to show (net-worth rollup, how PPF/FD/cash are entered-vs-imported, per-asset insight cards, multi-account switching, where loans live). Discovering a missing data dimension from a mockup is cheap; discovering it post-migration is not. This extends the principle already in the L1 spec (enriched expense reads were deferred to L2 'so the API is shaped by real intuitive-UI needs rather than guessed').

USER CHOICES for the spike (via AskUserQuestion): (1) SEQUENCING = "Standalone design spike now" — a dedicated UX/design brainstorm producing mockups + a design doc whose output becomes a NAMED INPUT to the later L1.5 spec and L2; PR #2 can merge in parallel. (2) FIDELITY = "Real visual mockups" — use the Figma/Stitch design tools (mcp__stitch__*, mcp__design__figma__*) to produce actual rendered screens, escalating cost accepted. NOT just ASCII wireframes.

SCOPE GUARDRAILS: This is the brainstorm phase only — output is mockups + a committed design doc at docs/superpowers/specs/YYYY-MM-DD-<topic>-design.md. NO code, NO core changes, NO building L2. Terminal state of the brainstorm is NOT writing-plans (that belongs inside the actual L1.5/L2 layers later) — it's a design doc that informs those future specs. Keep it a spike: if it drifts into 'let's build the React app', stop. Surface new platform ideas (net-worth trend, account comparison, loans placement) so they land in the data model BEFORE the L1.5 foundation is poured.

COVERAGE the spike should explore: unified investment / net-worth dashboard across all asset classes (MF, stocks, PPF, FD, cash); multi-account views + switching (D7 already in L1; L1.5 generalizes to expenses too); manual-entry vs file-import ingestion UX per asset class; per-asset insight cards; expense views (enriched reads were deferred to L2 precisely to design against concrete layouts); where loans fit. The AI/agent layer (L4) is the eventual product — keep a place for 'chat with your data' / wealth-manager insights in the IA.

HOW TO START NEW SESSION: SessionStart hook fires -> read MASTER_PLAN -> this is a design/UX task (T1-ish new surface but design-only; classify per myfinance-sdlc, likely treat as a brainstorm spike, confirm tier with user). Invoke superpowers:brainstorming. The brainstorming skill was already launched at the end of the prior session but the user chose to restart fresh — re-run it cleanly. Pending: also merge PR #2 (https://github.com/ak688744/MyFinance/pull/2) to main when ready (gh auth switch --user ak688744), then L1.5 proper after the design spike. Related decision: L1 complete id ab012766; L1.5 model/sequencing id 437133fd.
- Frontend/platform design spike complete (PR #3 merged) — IA + 6 mockups + L1.5 schema inputs locked — The standalone frontend/platform design spike (decided in c5a4e601) is DONE and merged to main as PR #3 (branch design/frontend-spike). Brainstorm-phase-only: no code, no core changes, did NOT proceed to writing-plans. Deliverable = docs/superpowers/specs/2026-06-13-frontend-design-spike-design.md + 6 rendered mockups in specs/mockups/ (01 networth-home, 02 investments, 03 investment-analyzer, 04 add-asset-fd, 05 expenses, 06 loans; + inspiration-expenses.png reference). Mockups built with Stitch (project 5629957510051795265, design system asset 2726046840844504940) and rendered crisply via headless Chrome.

APPROVED IA ("three pillars + AI", Option A): left sidebar Net Worth · Investments · Expenses · Loans · Assistant. Chosen because it maps 1:1 onto the L1.5 asset/liability schema AND the L4 agent split (investment/expense/loan specialists under a wealth-manager orchestrator).

PRODUCT IDENTITY: net-worth command-center (landing = one net-worth number + trend + asset-class composition + assets−liabilities; NOT chat-first, NOT a section launcher). Asset classes tracked: MF, stocks, PPF, EPF, NPS, FD, gold, real estate, cash (crypto/bonds deferred, enum stays open). Liabilities first-class (loans w/ EMI/rate/tenure/amortization) netted into net worth. Desktop-rendered, responsive; web first.

KEY OUTPUTS FOR L1.5 SCHEMA (doc §6 = the payload):
1. assetClass open enum.
2. valuationStrategy = market | computed | manual, VISIBLE in UI (badges + 'Updated N days ago' freshness chip for manual; manual needs lastValuedAt).
3. ingestionMode = file_import (L1 parser registry) | manual_entry (per-class CRUD form), orthogonal to valuation.
4. First-class accounts SCOPED PER-DOMAIN (per asset class / expense-vs-investment), NOT one global account list — DELIBERATE DIVERGENCE from the MASTER_PLAN L1.5 note that mused a single unified accounts model. L1.5 should model accounts per-domain.
5. First-class liabilities; net worth = Σ asset values − Σ liability outstanding.
6. Per-class computed inputs (FD: principal/rate/compounding/start/maturity/payout; PPF/EPF/NPS: balance/rate/contributions; gold: grams+stated; real estate: stated; loan: principal/rate/tenure/EMI→amortization).
7. AI insight slot as a data concern (text+severity+CTA) shared by UI cards and L4 agents.
8. NEW DIMENSION — historical net-worth / valuation snapshots: hero trend + per-class trend + loan payoff projection all need time series, absent from the MF-only model. L1.5 must decide snapshot-table vs derive-from-cashflows+price-history. (Clearest 'found a missing dimension in a mockup' win.)
9. NEW — budgets (Expenses shows '% of budget'/'Budget left'): per-category/overall monthly budget; L1.5 decides core vs API/UI.

PLATFORM PATTERN: AI is EMBEDDED + ON-DEMAND, not a persistent side panel — each screen has a contextual collapsible AI insight card + a floating 'Ask AI' button opening a chat drawer; drill-downs embed scoped 'ask a follow-up'; Assistant nav = full L4 home. (Decided during the Expenses iteration after the user rejected a persistent right rail as wasted space.) Expenses screen was iterated against a user-provided reference; final = full-width, KPI strip, inline collapsible AI insight, donut + simple month-on-month spend BAR chart (user rejected multi-line trend), recent txns with editable category chips + per-row account tags + inline auto-categorize 'correct?' affordance.

DESIGN SYSTEM intent for L2: calm/data-dense ('Bloomberg-calm meets modern fintech'); primary blue #1463F3; green #0E9F6E/red #E02424 ONLY for financial deltas; violet #7C5CFC reserved for AI; Manrope headings + Inter body, tabular numerals; ₹ lakh/crore formatting; white 12px cards on #F7F8FA.

STATE: L0+L1+spike all merged to main. NEXT = L1.5 Unified Investment Model as fresh T1 (Groww re-validation), using doc §6 as schema input and §3–§5 as L2 UI contract. Related: c5a4e601 (spike decision), 437133fd (L1.5 model/sequencing), ab012766 (L1 complete).
- L4 write-tools layer: small composable primitives (NO one-tool-per-intent, NO split tool), guardrails TBD — brainstorm not yet started — User wants the L4 agent to do everything they can do on the dashboard (categorize, add notes, split a transaction, etc.). Discussed 2026-07-15, NO code/spec yet — next clean session should run superpowers:brainstorming for the L4 write-tools layer. This is the write seam L3 deliberately reserved (tools/write/ empty + runInTransaction built-unused; see decision 0abb8b24).

TIER: T1 (new architectural surface — mutation tools + a safety model). NO Groww re-validation triggered — verified every dashboard write action is a thin wrapper over existing core repo/domain calls; NONE touch the Groww-frozen logic (categorize/xirr/portfolio/parsers). Lower-risk T1 than L1.5.

THE FULL DASHBOARD WRITE SURFACE (what "everything I do on the dashboard" means — all already exist as API routes + core fns):
- Transactions: POST /transactions (insertManual, self-generates unique dedupeKey `manual_<date>_<desc>_<amount>_<Date.now()>`), PATCH /transactions/:id (updateAmount/updateNote), DELETE /transactions/:id, PATCH /transactions/:id/category (updateCategory + optional saveCategoryMemoryRule merchant/keyword + recategorize).
- Categories/rules: POST/PATCH/DELETE /categories(/:id); POST/PATCH/DELETE /categories/rules(/:id); POST /recategorize; POST /categories/ai-suggest.
- Accounts: POST /accounts.
- Assets: POST/PATCH/DELETE /assets(/:id); POST /assets/:id/{contributions,valuations,rates}.
- Liabilities: POST/PATCH/DELETE /liabilities(/:id).
- Imports: POST /imports/{expenses,investments/holdings,investments/transactions} — file-upload; probably stay dashboard-only (don't fit chat tools) unless user wants agent ingesting files.

KEY DESIGN DECISION (user's explicit stance): PREFER a small set of COMPLETE, COMPOSABLE write primitives + good tool descriptions over one-tool-per-user-intent. Specifically NO dedicated split_transaction tool — the agent composes it from update_transaction (shrink original) + add_transaction (remainder). User: "I would want my agent to be smart enough to figure out how to use existing tools." This keeps the catalog small (L3 spec: context window is the scarce resource). Same primitives also yield move-to-another-account, duplicate-for-next-month, etc.

SPLIT feasibility RESOLVED (no schema change needed): the transactions table has NO parent_id/split column and dedupe_key is NOT NULL UNIQUE, so a native "split" column would be a schema+core change. BUT user's approach — updateAmount(original, partA) + insertManual(partB copying date/desc/direction/account) — needs ZERO schema/core change. insertManual self-generates a unique dedupeKey so programmatic second insert won't collide; original keeps its dedupeKey so a re-import won't resurrect the pre-split amount.

TRADEOFFS TO PIN DOWN IN THE BRAINSTORM/SPEC:
1. ATOMICITY: composing split as two separate agent tool-calls loses the all-or-nothing a dedicated runInTransaction-wrapped tool would give — update can succeed then add fail, leaving money missing. Mitigation: agent reads back to verify; OR reconsider a transactional wrapper only where multi-write atomicity truly matters.
2. GUARDRAILS (the real design work, since schema is a non-issue): explicit opt-in flag to enable writes at all (spec D8); confirmation/dry-run semantics (preview before mutate?); scoping caps (recategorize_all + rule creation can touch hundreds of rows — bound it); split sum-guard (enforce parts sum to original → handler-level isError like L3 unknown-id).
3. Primitives must be COMPLETE enough to compose (add_transaction already accepts date/desc/amount/direction/categoryId/note/accountId — good). 
4. Tool descriptions should TEACH the multi-step recipes (description-as-documentation, same technique L3 used for the xirrFraction footgun) — e.g. add_transaction description mentions the split recipe.

PROPOSED WRITE-TOOL SET (~18 composable primitives, refine in spec): add_transaction, update_transaction(amount/note), delete_transaction, categorize_transaction(+learnRule); create/rename/delete_category; create/update/delete_rule; recategorize_all; create_account; add/update/close_asset + add_asset_contribution/valuation/rate; add/update/delete_liability. Imports deferred.

Build over the L3 packages/mcp seam (tools/write/*), direct-to-core, reusing the reserved runInTransaction. L3 is PR #12 (merge first). Related: L3 build decision 0abb8b24.
- Feature B (Expense AI Insights + Notes→Tags + Expense Clarity Agent): T1 spec+plan DONE, build NEXT via subagent-driven-development on feat/expense-ai-insights-tags — Brainstorm+plan complete 2026-08-05 (T1, no frozen financial logic → Groww 6/6 gate, no re-validation). Spec docs/superpowers/specs/2026-08-05-expense-ai-insights-tags-design.md (commit d2ab980); plan docs/superpowers/plans/2026-08-05-expense-ai-insights-tags.md (24 tasks, 4 phases A/B/C/D). NO code yet. Branch feat/expense-ai-insights-tags off feat/agent-response-calibration (user chose to base on it — reuses ask_user/QuestionChips machinery, since calibration is pushed but NOT merged to main).

LOCKED DECISIONS (all user-approved via AskUserQuestion this session):
- SCOPE: three subsystems in ONE build — A tags model, B insight detectors, C Expense Clarity Agent + sidebar chat + confidence-gated categorization. DROPPED bulk "Suggest tags with AI" pass (tags = user's stated INTENT, captured in chat, not bulk-guessed — a category is inferable from merchant but "is this recurring?" needs the user). DEFERRED (own future T1): E = transaction vectorization/RAG; web_search tool+provider; recurring-not-tagged detector; auto-run-on-import.
- A TAGS MODEL: tags TEXT JSON column on transactions (migration 0007), array of {tag, source:'user'|'agent'} — NO junction table (user challenged it; dominant access pattern is "read transactions, tags ride along"; JSON mirrors how note col was added 0005). note column STAYS for free-text nuance. Pure helpers in core/domain/tags.ts (normalizeTags lowercase/trim/dedupe last-source-wins, parseTags/serializeTags null-when-empty, mergeTags/removeTagFrom). Repo getTags/setTags/addTags/removeTag + tags on ExpenseTransactionRow.query(). API PATCH/DELETE /transactions/:id/tags (source='user'). MCP tag_transaction write tool (source='agent', NOT preview-gated, count 22→23). Web tag chips per row, agent-styled distinct (violet) from user.
- B INSIGHTS: pure core/domain/insights/expenseInsights.ts, 3 deterministic detectors (needs_clarity=uncategorized OR vague[deriveMerchantName null] AND untagged, grouped one card; new_spend=merchant not in prior 3mo, ≥₹500 floor, top-5; abnormal_spend=category M>1.4×trailing-3mo-avg AND jump≥₹1000, warn). Named-constant thresholds. deriveMerchantName INJECTED (stays out of core; api ports it to api/src/lib/). GET /expenses/insights?month= computed on-load, NO LLM, TanStack-cached, self-heals on tag/categorize invalidation. localStorage dismissal by stable insight id. Reserves the L2-spike "AI insight slot (text+severity+CTA)".
- C EXPENSE CLARITY AGENT: user reframed from "reuse wealth agent" to a dedicated single-responsibility Expense Agent whose mission = TOTAL CLARITY OVER EVERY RUPEE (categorize→disambiguate→tag). buildExpenseAgent in agent-harness, ISOLATED memory resource 'expense-agent' (no pollution of wealth advisory recall pool — the known pollution vector), FOCUSED toolset (allowlist: finance_list_transactions/get_expense_summary/list_categories/categorize_transaction/tag_transaction/create_category/create_rule + ask_user; NOT investment/loan/networth). OWN expense_agent AI task (independently routable/priced in AI Settings, likely cheaper model than Opus wealth_chat). runChat gains agent:'wealth'|'expense' param selecting builder/route(resolveRoute(task) generalizes resolveWealthRoute)/memory-resource. Reached via a NEW list_categories MCP read tool (10→11; agent can't categorize without category ids). Sidebar chat: extract shared ChatPanel from AssistantPage, new ExpensesInsightDrawer (fresh non-persisted thread, client-composes buildInsightSeed message → existing POST /agent/chat + agent field, NO new context contract). This is an early slice of L4.1's planned expense specialist (note in MASTER_PLAN so L4.1 doesn't duplicate).
- CATEGORIZATION CHANGE (behavior change, intentional, tested): categorizeWithAI already parses confidence; add minConfidence default 0.9 → drop below-threshold (LEAVE BLANK, departs from today's always-assign ai-suggest). On confident apply where extractMerchantKey derivable, ALSO create a merchant rule (model-knowledge + rule-writing is the categorization strategy — NOT vectors). Left-blank txns feed needs_clarity insights → chat → resolve. NOT a frozen-logic change: categorizeWithAI/ai-suggest live in packages/agents (non-frozen); domain/categorize.ts byte-identical.

RAG DECISION (user asked "does our architecture support RAG / would it make sense to vectorize transactions"): current transactions have NO vector storage (grep vector/embedding/f32 in core schema = nothing); the only vector infra is Mastra agent-MEMORY message embeddings (LibSQLVector+fastembed) in the separate demo.db.memory.db, NOT finance data. Transaction matching is mostly LEXICAL (merchant strings) which rules+extractMerchantKey already handle; rules table IS an exact/free/explainable memory. So vectors deferred and, when built, justified PRIMARILY by semantic search/grouping/insights + long-tail residue, NOT as core categorization — aligns with MASTER_PLAN pgvector-in-same-DB arc.

TRIGGERS (all user-initiated v1): insight-card click→chat; "Suggest with AI" categorize; manual tag chips. Insights compute on load with NO LLM. No auto-run-on-import.

NEXT: execute via superpowers:subagent-driven-development, fresh subagent per task, Task A1 first. GOTCHAS: Node 22 (nvm use 22); NEVER pnpm install (crashes) — build/test via node_modules/.bin/{tsc,vitest}; tsx never compile; tsc --build graph order core→agents→api/mcp/agent-harness→web; push gh auth switch --user ak688744. Related: Feature A (CC upload) separate session; response-calibration session 2026-08-04.
- Feature B builds on the SAME branch feat/agent-response-calibration (no separate feature branch) — supersedes the branch note in decision 29b52df7 — User decision 2026-08-05: build Feature B (Expense AI Insights + Notes→Tags + Expense Clarity Agent) directly on the existing feat/agent-response-calibration branch — do NOT create feat/expense-ai-insights-tags. That branch already carries the calibration/ask_user work; Feature B commits stack on top of it (HEAD before Feature B = commit b02051e). One branch, one eventual PR into main covering BOTH the calibration feature and Feature B. Spec 2026-08-05-expense-ai-insights-tags-design.md (d2ab980), plan 2026-08-05-expense-ai-insights-tags.md (updated for same-branch). Code-review diff base for close-out = b02051e. Everything else in decision 29b52df7 stands (24-task plan, phases A/B/C/D, all design decisions). NEXT: execute via superpowers:subagent-driven-development, Task A1 first, on feat/agent-response-calibration. Node 22; never pnpm install; node_modules/.bin/{tsc,vitest}; tsx never compile; push gh auth switch --user ak688744.
- Credit-card bill split feature: brainstorm started, classified T2 (user override of my T1), build in worktree off origin/main — Starting Feature A (credit-card bill upload/split) 2026-08-08. Design handoff at /Users/vkhandelwal/Downloads/design_handoff_credit_card_split (README.md + 2 .dc.html mockups). I classified T1 (new PDF parser + touches frozen categorize.ts detection); USER OVERRODE to T2 with the guardrail that we keep domain/categorize.ts BYTE-IDENTICAL and don't edit an existing validated parser (CC-bill detection = additive signal/rule, CC PDF parser = net-new code with no Groww golden master to regress). If forced into frozen engine, that slice rounds back to T1 + Groww re-validation. Another session holds branch feat/agent-response-calibration, so build in a WORKTREE off origin/main.

DESIGN (from README): (1) detection at categorization time sets a credit_card_bill signal/categorySource → UI renders 'Credit card bill' badge + upload button on the ledger row. (2) Upload modal → parse PDF → NO confirm-preview → auto-create child transactions → open inline split/categorize panel. (3) Split panel: reconciliation bar (green matched / amber mismatch delta), line-item rows each with CategoryChip (reuses useUpdateTxCategory), '+ Add missing line', 'Confirm split'. (4) Post-split: parent collapses to dashed 'container' row EXCLUDED FROM MONTH TOTALS/KPIs; children render indented, independently editable. State model per parent: status flagged|parsing|split|done, linkedChildIds, billAmount, parsedTotal(derived), matched(derived). Children get parentTransactionId FK back to bill row.

KEY BACKEND FACTS gathered: transactions table (packages/core/src/db/schema.ts) has no parent_id/split column and no CC flag — needs schema add (migration 0009). categorySource is free TEXT (no CHECK) so a new 'credit_card_bill' value is additive/safe. Import registry at packages/api/src/import/registry.ts REGISTRY{hdfc:{expense},groww:{...}} — CC parser is drop-in (add e.g. {hdfc_cc:{expense:parseHdfcCcStatementPdf}}). Existing parsers are XLS (parseHdfcStatementXls); CC statements are PDF → NEW pdf-parse dependency. expenseTxRepo.summary() computes totalSpent/byCategory/byMonth — parent must be excluded once split (add a WHERE cond, e.g. exclude rows with a split marker/categorySource='credit_card_bill' that are done, and DON'T double count children+parent). insertManual self-generates unique dedupeKey. Node 22; never pnpm install; node_modules/.bin/{tsc,vitest}; tsx never compile; push gh auth switch --user ak688744.
- CC-bill-split plan complete; worktree REBASED onto feat/agent-response-calibration (Feature B needed); migration 0009 — Feature A (credit-card bill split) spec+plan DONE 2026-08-08 in worktree .claude/worktrees/credit-card-split, branch feat/credit-card-bill-split. Spec docs/superpowers/specs/2026-08-08-credit-card-bill-split-design.md; plan docs/superpowers/plans/2026-08-08-credit-card-bill-split.md (13 TDD tasks). NO code yet.

KEY BASE-BRANCH DECISION (user via AskUserQuestion): worktree was created off origin/main but REBASED onto feat/agent-response-calibration because Feature A's detection (credit_card_bill TAG) + agent auto-tagging need Feature B machinery that is NOT on main (tags column, domain/tags.ts, buildExpenseAgent/EXPENSE_INSTRUCTIONS, tag_transaction MCP tool, migrations 0007/0008). Rebase was CLEAN (my 2 doc commits replayed on top of calibration tip 9b2bc7d). Consequence: this branch STACKS on calibration+Feature B, so its eventual PR carries all of that too (or merge calibration first). Another session actively works the calibration branch in the MAIN working dir — I kept my own branch and never checked out theirs.

PLAN SHAPE (T2, keep categorize.ts BYTE-IDENTICAL, Groww 6/6 standing gate): Task0 add pdfjs-dist dep (install via node ~/.cache/node/corepack/v1/pnpm/10.4.1/dist/pnpm.cjs install --config.manage-package-manager-versions=false) + migration 0009_credit_card_split.sql (ALTER TABLE transactions ADD parent_transaction_id integer REFERENCES transactions(id) + idx_transactions_parent; journal idx 9). Task1 seed credit_card_bill starter category. Task2 repo insertChild/listChildren + parentId query filter + parentTransactionId on ExpenseTransactionRow (dedupeKey cc_<parent>_..._<rand>). Task3 summary() excludes split parents via NOT IN (SELECT parent_transaction_id WHERE NOT NULL) appended to windowConds+spendConds. Task4 agents cc_statement_parse task + parseCcStatement(complete,text) mirroring categorizeWithAI (Zod CcStatementParseSchema {lineItems:[{date,merchant,amount signed}], detectedTotal:number|null}, retry-once, rethrow auth/provider_not_configured, throw 'cc_parse_failed'). Task5 api/lib/pdfText.ts extractPdfText(buffer,password?) via pdfjs-dist/legacy/build/pdf.mjs + PdfPasswordRequiredError/PdfPasswordIncorrectError (password never logged/persisted). Task6 api/lib/ccSplit.ts reconcile() = matched when |parsedTotal - (detectedTotal ?? parentAmount)| < 1, reconciledAgainst statementTotal|billAmount, carryover=parentAmount-(detectedTotal??parsedTotal). Task7 POST /transactions/:id/split-from-statement (extend transactionRoutes signature to (app,{gateway}), wire in server.ts; getFullById repo read for parent amount/accountId; refund negative→credit child; insertChild in runInTransaction; 404/409-already-split/400 password&parse errors, 502 auth). Task8 expenseAgent EXPENSE_INSTRUCTIONS add step to tag credit_card_bill. Task9 web types SplitResult + useSplitFromStatement (apiUpload multipart) + UploadIcon. Task10 SplitStatementModal (file + optional password). Task11 SplitPanel (reconciliation bar green/amber + carryover line + CategoryChip line items). Task12 ExpensesPage wire isCreditCardBill (categoryId==='credit_card_bill' || tag) + isSplitContainer + badge/upload/panel/dashed-container-row. Task13 full-suite + guardrails (categorize.ts git-diff EMPTY, Groww 6/6, seam clean) + MASTER_PLAN/memory close-out.

RECONCILIATION MODEL (user-refined): LLM keeps refunds as NEGATIVE line items (don't drop — would overstate spend + break totals), excludes ONLY prior-bill-payment line; reconcile parsedTotal vs statement's own detectedTotal (NOT bank-paid amount, because carryover); carryover surfaced as informational muted line, never red. PDF parse = LLM via gateway (user chose over deterministic parser).

NEXT: execute via superpowers:subagent-driven-development, Task 0 first, in the worktree. Node 22; never pnpm install; node_modules/.bin/{tsc,vitest}; tsx never compile; push gh auth switch --user ak688744. Supersedes brainstorm-start decision 05486149.
- CC-bill-split BUILT + reviewed + fixed + pushed (feat/credit-card-bill-split); 3 review findings fixed — Feature A (credit-card bill split) BUILT by a separate executing agent, then reviewed + fixed + committed + pushed by me 2026-08-08. Branch feat/credit-card-bill-split pushed to origin (ak688744/MyFinance), stacks on feat/agent-response-calibration (unmerged — carries Feature B). PR not yet opened. 6 clean per-layer commits (core/agents/api/agent-harness/web/docs) on top of the spec+plan commits.

REVIEW VERDICT: faithful to spec+plan. Full suite green: core 285, agents 54, api 106, agent-harness 51, web 78. T2 guardrails held: categorize.ts BYTE-IDENTICAL vs base (git diff empty), Groww golden 6/6, seam clean (grep hits were comments only). tsc --noEmit clean web+api (note: tsc --build hits a NON-code EPERM writing packages/core/dist/test/fixtures/growwGolden.json — a sandbox/xattr quirk on the fixture-copy step, unrelated to feature; tests run via tsx so unaffected).

3 FINDINGS FOUND & FIXED before commit:
1. (REAL BUG) apps/web ExpensesPage: allRows = txns.data (paginated page, PAGE_SIZE=25) was used for isSplitContainer + children lookup. Parent bill (mid-month) and children (spread across month) land on different pages → parent renders as plain row not container, children (filtered out of topLevelRows by parentTransactionId!=null) render on NO page (invisible) after refresh. FIX: allRows = fullMonthTxns.data ?? txns.data (fullMonthTxns limit-1000 whole-month query already existed for insights). topLevelRows still from paginated displayedRows. Totals were always correct (summary is separate core SQL).
2. (TEST GAP) pdfText password tests were silent no-ops (early return when fixtures/encrypted.pdf absent) → password branches untested. FIX: makePdf.ts gained makeEncryptedPdf() — generates a REAL RC4 (rev-2, 40-bit, Standard security handler) password-protected PDF in pure JS (Node 22 OpenSSL disables rc4 via createCipheriv → ERR_OSSL_EVP_UNSUPPORTED, so RC4 is hand-implemented; MD5 via node:crypto is fine). Verified against real pdfjs: no-pw→PasswordException code1 (NEED_PASSWORD), wrong-pw→code2 (INCORRECT_PASSWORD), correct-pw→extracts 'SWIGGY 500'. pdfText.test.ts rewritten to assert all 4 real cases + corrupt-buffer→pdf_extract_failed (api 104→106 tests).
3. (COSMETIC) migration 0009 journal 'when' was 1785000000000 < 0008's 1786000000000 (out of order; Drizzle applies by idx so harmless). FIX: bumped to 1787000000000.

DESIGN AS BUILT (matches spec 2026-08-08-credit-card-bill-split-design.md): migration 0009 parent_transaction_id self-FK + idx; repo insertChild (dedupeKey cc_<parent>_..._<rand>, sourceType='cc_statement')/listChildren/getFullById + parentId query filter + parentTransactionId on ExpenseTransactionRow; summary() excludes parents-with-children via NOT IN (SELECT parent_transaction_id WHERE NOT NULL) on windowConds+spendConds; seed credit_card_bill starter category; agents cc_statement_parse task + parseCcStatement (Zod, signed refunds negative, detectedTotal, retry-once, rethrow auth/provider_not_configured, throw cc_parse_failed); api/lib/pdfText.ts extractPdfText via pdfjs-dist/legacy/build/pdf.mjs (+optional password never logged, PdfPasswordRequired/IncorrectError); api/lib/ccSplit.ts reconcile (matched=|parsedTotal-(detectedTotal??parentAmount)|<1, carryover); POST /transactions/:id/split-from-statement (getFullById parent, 404/409-already-split/400 password&parse/502 auth, refund→credit child, insertChild in runInTransaction, resolveCategoryFromRules auto-categorize); expenseAgent EXPENSE_INSTRUCTIONS step 4 tags credit_card_bill; web types SplitResult + useSplitFromStatement + UploadIcon + SplitStatementModal (+password field) + SplitPanel (reconciliation bar + carryover line + CategoryChip) + ExpensesPage isCreditCardBill/isSplitContainer + badge/upload/container rows.

NEXT: open PR (user call) — either merge calibration/Feature B first, or this PR carries all of it. Node 22; never pnpm install; node_modules/.bin/{tsc,vitest}; tsx never compile. Supersedes plan decision dd7d3e71.

## Unresolved
- L0 build PARKED at Task 2.2 done — resume from Task 2.3 (schemeRepo + holdingsRepo) — L0 Foundation build paused 2026-06-12, mid-execution. Resume point for next session.

BRANCH: layer/0-foundation (NOT merged). 13 commits, HEAD=dbc29e6. All committed, working tree's uncommitted files are PRE-EXISTING RN app files (not L0 work — ignore them).

PLAN: docs/superpowers/plans/2026-06-12-l0-foundation.md. SPEC: docs/superpowers/specs/2026-06-12-l0-foundation-design.md. Executing via subagent-driven-development (fresh subagent per task; spec+quality review after logic tasks).

DONE (Phases 0-1 + start of Phase 2):
- 0.1 pnpm workspace + stubs; 0.2 core skeleton; better-sqlite3 native build whitelisted (root package.json pnpm.onlyBuiltDependencies); Node 20 pinned (.nvmrc).
- 1.1 Drizzle schema (8-table faithful port, REAL money, +5 SQL CHECK constraints via check()); 1.2 client.ts (better-sqlite3, driver isolated); 1.3 drizzle-kit migrations + runMigrations(path?).
- 2.1 repo interfaces (repositories/types.ts) + shared domain types (types.ts), verbatim from RN services.
- 2.2 investmentTxRepo (makeInvestmentTxRepo(db)) + 23 tests. Full suite 27/27 green.

RESUME AT Task 2.3: schemeRepo + holdingsRepo. Then 2.4 (category/rule/expense-tx/importHistory repos), Phase 3 domain port (3.1 xirr verbatim, 3.2 returns/portfolio repo-injected, 3.3 categorize, 3.4 nav+amfi), Phase 4 (4.1 characterization, 4.2 golden-master vs Groww — oracle data already captured in memory id 8c4e7836), Phase 5 import, Phase 6 Fastify API, Phase 7 close-out.

KEY GOTCHAS for subagents: (1) Node 20 mandatory — prefix cmds `source ~/.nvm/nvm.sh && nvm use 20 && ...`. (2) repo methods are SYNCHRONOUS (better-sqlite3) — domain port must drop await; only NAV (fetch) stays async. (3) Drizzle select() is UNORDERED — any logic relying on natural/chronological row order MUST add explicit ORDER BY (caught a real unit-accumulation regression in 2.2). (4) seed parent investment_schemes rows before investment_transactions in tests (foreign_keys=ON).

## Setup
- Initialized project-memory for MyFinance — Initial setup for local project memory

## Tooling
- project-memory setup complete for MyFinance — MCP server, hooks (SessionStart/PreToolUse/PostToolUse/Stop), daemon, and code graph (36 files, 200 nodes, 773 edges) are all active. Configured via .mcp.json + .claude/settings.local.json. Dashboard running at http://localhost:3777.

## Architecture
- undefined
- Task 3.4: navService ported verbatim to packages/core/src/domain/nav/; amfiMatcher pure scoring verbatim, autoMatchAmfiCodes repo-injected — navService is already a pure global-fetch client (cache/TTL/DD-MM-YYYY parsing preserved exactly). amfiMatcher.autoMatchAmfiCodes no longer takes a db+SQL; takes deps:{schemeRepo}. Added SchemeRepo.getUnmatchedSchemes() (SELECT WHERE amfi_code IS NULL ORDER BY scheme_name) for faithfulness to the original SELECT rather than filtering getSchemes(). force option accepted for parity but ignored (no bulk-clear in repo layer).
- core package public surface: index.ts barrel; ImportTransactionsResult collision resolved by renaming the importInvestmentTransactions variant to ImportInvestmentTransactionsResult on re-export — importTransactions.ts and importInvestmentTransactions.ts both export ImportTransactionsResult with different shapes; TransactionType is identical in types.ts and transactionParser.ts so re-exported from types only. seedDatabase(db) ports the RN initializeDatabase seed step (upsertStarter all starterCategories, then recategorizeNonManualTransactions), synchronous + idempotent via INSERT OR IGNORE.

## Validation
- Groww golden-master gate passes: ported XIRR/returns math reproduces real per-scheme Groww XIRR within 0.046pp (tolerance 0.5pp) for all 6 schemes — Core-logic T1 validation gate for L0 Foundation. Per-scheme getPeriodReturns(ALL) with NAV stub (currentNav as terminal) and today=asOfDate. Max diff 0.046pp (SCHEME_A), confirms the L3 returns.ts port is faithful to the RN original. No domain code modified; tolerance untouched. Commit 9df1a82.

## L1-api-multipart
- First-file-wins multipart helper with ArrayBuffer cast — L1 Task 2: readMultipart() implements first-file-wins by checking if(file===null) before assigning, and drains ignored file parts via toBuffer(). All text fields collected even after first file. nodeBuffer.buffer.slice() requires 'as ArrayBuffer' cast for TypeScript (SharedArrayBuffer vs ArrayBuffer union), but runtime is unchanged. @fastify/multipart augmentation loaded via import. Typecheck clean. Commit 9df4136.

## L1.5-build
- L1.5 plan Task 4 computed-valuation test had a leap-year day-count defect; resolved by fixing test expected-values to spec's days/365 convention, implementation kept verbatim — The plan's Task 4 test hard-coded whole-year exponents (Math.pow(1.02,4)) and 730-day spans, but the spec prose mandates yearsBetween=days/365 and 2024 is a leap year (366 days; 2023-01-01..2025-01-01=731 days). Verbatim test vs verbatim impl were mutually inconsistent for any window spanning Feb 2024. Resolution (a): keep computed.ts exactly as specified (days/365, the Groww-validated XIRR day-count), correct only the test EXPECTED values to 366/365, 731/365 etc. Preserves closed-form property; financial behavior unchanged. Task 16's test already used 366/365 correctly, confirming day-counting was the intended convention. All 7 computed tests green.

## L1.5-complete
- L1.5 Unified Investment Model BUILT and shipped as PR #4 (branch layer/1.5-unified-investment); MF pipeline untouched, Groww golden-master unchanged — Executed the 30-task TDD plan via subagent-driven-development. Delivered: 6 new core tables (accounts/assets/asset_contributions/asset_rates/asset_valuations/liabilities) + nullable transactions.account_id (migration 0001_round_glorian.sql); valuation engine domain/valuation/ (rate-period compound interest FD/PPF/EPF/NPS, manual stated-value+ageDays, valueAsset dispatcher); domain/loans/amortization.ts (computeEmi/amortizationSchedule/loanStatus); domain/networth/networth.ts (getAllAssets read-time union projection of untouched MF holdings + generic assets, getNetWorth=Σassets-Σliab, getNetWorthHistory derive-on-read); 6 repos; expense account_id+updateAccount; API routes /accounts /liabilities /assets(+sub) /networth(+/history); MF import ensureAccount (non-financial). T1 GATE PASSED: Groww golden-master 6/6 unchanged, core 215 + api 42 green, typecheck clean tsc --build, seam invariant clean, rollup-continuity (MF-only getNetWorth.totalAssets==getPortfolioSummary.totalCurrentValue) + textbook-oracle math. Code review READY TO MERGE. GOTCHAS: (1) plan Task-4 test had leap-year day-count defect (whole-year exponents vs spec days/365); fixed TEST expected values to 366/365 & 731/365, impl verbatim. (2) loanStatus boundary: payment due exactly today = next due not paid (dueDate<today paid, >=today remaining). (3) extending ExpenseTransactionRepo.insertIgnore with required accountId broke importTransactions caller + 2 existing tests + categorize fake repo — all updated (accountId:null for expense, which has no account dimension yet). NEXT=merge PR #4 then L2 Web UI.

## Bug fixes
- L2 manual-test bug batch (T2): fixed 6 web/api bugs + 1 doc fix on layer/2-web-ui — BUG-003 apiSend set Content-Type:application/json even with no body -> Fastify 400 on body-less DELETE; fix: only set CT/body when body provided. BUG-005 GET /liabilities returned emiAmount=null for tenure-based loans; fix: enrich list with computed emi via core computeEmi (no core change). BUG-002 InvestmentsPage double-counted MF (both /holdings and /assets project MF); fix: groupHoldingsByClass skips assetClass==mutual_fund generic assets. BUG-001 KPI strip MF-only; fix: relabel as MF portfolio + add all-assets total KPI. BUG-006 secondary error cards lacked Retry; fix: onRetry on Investments holdings/assets + Expenses recent txns. BUG-008 no expense add/import entry; fix: +Import expenses button launches ImportModal (defaults expenses). BUG-004 seed/expectation: swiggy@axis-order has no builtin food keyword by design; fix test plan seed to -breakfast + clarifying note (no core change). BUG-007 visual parity deferred per user.
- Portfolio XIRR showed 0.1% — UI fraction-vs-percent unit bug, NOT an XIRR math error (T3) — User imported real Groww MFs, portfolio XIRR rendered 0.1%. Reproduced getPortfolioSummary against the live demo.db with live NAV: core returns xirr=0.0949 (a FRACTION) which equals 9.49% — matches Groww reported 9.43%. Per-scheme XIRRs all sensible (11.38/6.70/5.93/-2.30/-4.88/2.89). Root cause: getPortfolioSummary.xirr and getPeriodReturns.xirr are raw FRACTIONS (Groww golden-master asserts abs(xirr-growwPct/100)<=0.005), but getHoldings.returnsXirr is pre-scaled (xirr*100). Frontend formatPercent expects percentage scale, so 0.0949 rendered as 0.1%. Fix: InvestmentsPage.tsx Portfolio XIRR KPI multiply summary.data.xirr*100 before formatPercent. Only that one site was wrong; per-holding returnsXirr + returnsPercent already percentages; useReturns hook unused in UI. No core change, Groww gate untouched. NAV network fetch confirmed WORKING (mfapi).

## UI
- BUG-007 visual parity (T2): rebuilt app shell + all 4 data screens to mockup parity, real data only — Added icons.tsx (inline SVG set, no icon dep), TopBar.tsx (title/subtitle/search-placeholder/bell/avatar), upgraded Sidebar (icons+Wealth Manager subtitle+Settings/Support footer+avatar). App.tsx Layout derives per-route title for TopBar + scroll container. NetWorth=command-center grid (hero+performance, composition bar+legend grid + Summary card, per-class mini-cards). Investments=account filter chips + MF full table (invested/current/returns/XIRR cols) + generic-class group cards; KPI strip. Expenses=table cols (merchant/category-chip/account/date/amount) + savings-rate KPI. Loans=summary KPI strip + LoanCard (per-loan useLiability for outstanding/next-due/progress bar). Omitted (no data, honest): net-worth deltas, sparklines, debt-payoff projection, budgets. No core/API change; web typecheck clean.
- Expenses transactions table redesign — inline row editing, own branch off main, phased — Redesigning the Expenses transaction table (T3 design + implementation). Stitch mockup v2 saved to docs/superpowers/specs/mockups/07-expenses-transactions-table-redesign.png shows: filter bar (search wired to ?search=, Category multi-select, Account, Direction, Date; active-filter pills + Clear all; Uncategorized-only + AI-suggested toggles), two-line merchant (name + raw desc), sortable Date/Amount headers, better pagination, INLINE ROW EDIT (amount input + note input + category dropdown + Save/Cancel; merchant & date stay read-only because no edit endpoint exists), hover quick-actions (edit/delete/note), bulk Categorize+Delete bar. User DROPPED amount min/max filter and Export. Edit UX = INLINE (not modal/drawer). Editable fields map to existing endpoints: amount+note via PATCH /transactions/:id, category via PATCH /transactions/:id/category, delete via DELETE /transactions/:id. Build in a git WORKTREE on new branch off main (another agent working on layer/3-mcp concurrently — keep isolated). Phases: P1 frontend-only (search/filters/pills/toggles/two-line merchant/sortable/pagination/inline-edit/hover-actions), P2 multi-select category filter (query() takes single categoryId today — add categoryIds[] IN), P3 bulk categorize/delete. Split-transaction deferred (no schema).

## UI/expenses
- Expense screen redesign (T2): spent excludes investment+transfer, Saved=Invested+Cash; month selector w/ prev-3/next-3 MoM window; compact bar labels — User flagged that Total Spent double-counted investments. Model locked with user: expenseTxRepo.summary() gained excludeFromSpend[] (default ['investment','transfer']) + investmentCategories[] (default ['investment']) params -> new 'invested' field; totalSpent/totalIncome/byCategory/byMonth all drop excluded categories; uncategorized(NULL) debits ALWAYS remain spend (SQL NULL NOT IN(...) is NULL not true, so exclude cond is OR(isNull, notInArray)). API /expenses/summary injects those defaults, overridable via ?excludeFromSpend=&investmentCategories= CSV (empty string = opt out). Saved = Income - TrueSpend = Invested + CashRemaining shown as sub-parts on KPI card. Frontend: month selector defaults currentMonth() with prev/next arrows (next disabled at current); KPIs+donut scoped to selected month, MoM bar chart uses SEPARATE all-time summary windowed via monthWindow(all, selected, span=3) = 3-before/selected/3-after shifting to stay in-data. Bar chart shows compact value (formatCompactShort ₹42K/₹4.2L) on TOP of bar via LabelList, exact ₹ on hover; donut labels slices >=6% on-chart with name+%. Category filter dropdown + offset pagination (PAGE_SIZE=25). Account column resolves accountId->'institution · label' via useAccounts('expense'). NO core financial logic touched (XIRR/categorize/portfolio untouched), so no Groww re-validation. Core 11 + api 4 expense tests + web 17 format tests green. KNOWN GAP: expense txns all have accountId=null (HDFC import never assigns account) + zero expense-domain accounts exist, so Account column shows '—' until import-side account linking is built (separate change). Uncategorized category-filter option dropped (API eq() cant match NULL).
- Expenses Merchant column shows a derived merchant name (deriveMerchantName in apps/web/src/lib/format.ts) with fallback to full raw description; each merchant cell is a click-to-expand/collapse button revealing the full transaction text (MerchantCell in ExpensesPage.tsx) — T2 web-only change; deriveMerchantName is the display-oriented sibling of core extractMerchantKey (parses UPI/ACH/POS structured formats, title-cases, returns null when no useful merchant -> caller falls back to full description). No core/API change, no Groww re-validation. 7 unit tests added to format.test.ts.
- undefined

## Api-architecture
- Added badRequest/notFound/conflict helpers to packages/api/src/errors.ts as exportable utilities instead of local-per-file definitions — Multiple route files were defining the same helper functions locally. Centralizing in errors.ts provides consistency, reduces duplication, and matches API house style for error handling with statusCode property.

## Web-ai-hooks
- AI settings hooks follow TanStack Query pattern with typed apiGet/apiSend, query key factory, invalidation matching existing useRules/useUpdateTxCategory — Task 18 adds AI settings hooks. Studied hooks.ts patterns: useQuery wraps apiGet, useMutation wraps apiSend with queryClient.invalidateQueries onSuccess. Query keys via qk factory (ai.providers, ai.models(providerId), ai.tasks, ai.usageSummary(range), ai.usageEvents(filters)). Mutations invalidate relevant keys (createProvider invalidates ai.providers; setTaskRoute invalidates ai.tasks). PricingHint as query helper returning nullable DTO. Matches house style exactly.

## Expense-enhancements
- Expense platform enhancements: added note column to transactions, CRUD endpoints (POST/PATCH/DELETE /transactions), and changed DEFAULT_EXCLUDE_FROM_SPEND from 'transfer' to 'self_transfer' — T2 backend-only change. Added nullable note TEXT column to transactions table (migration 0005); extended ExpenseTransactionRepo with updateAmount/updateNote/deleteTransaction/insertManual methods; added POST /transactions (manual entry), PATCH /transactions/:id (update amount/note), DELETE /transactions/:id API endpoints; query() now returns note field. Changed spend calculation to exclude 'self_transfer' instead of 'transfer' so bank-to-bank transfers count as spend/income. No core financial logic touched (XIRR/portfolio/categorize untouched). Fixed test mock in categorize.test.ts to include stub implementations of new repo methods.

## L4-wealth-agent
- undefined
- Wealth-agent verbosity+wrong-number bug was MEMORY POLLUTION, not just prompt; repaired working memory + added memory-hygiene prompt rule (T2) — ROOT CAUSE isolated live: the persistent 20k-char dump + wrong surplus 127,915 + 'Perfect balance' reproduced ONLY with the real demo.db.memory.db, NOT with clean :memory: (clean run = 439 chars, agent fetched live data + asked clarifying Q). The poison lived in mastra_resources.workingMemory (whole computed FINAL MONTHLY BUDGET plan incl wrong 127,915 surplus, missing existing-SIP line, 'Perfect balance 100% deployed') + 11 mastra_messages. It was re-injected every turn (workingMemory always injected; semanticRecall resource-scoped='user' constant) so the model 'refreshed' the saved plan verbatim instead of recomputing. ARITHMETIC TRUTH from data: income 348384 - fixed 141969 - discretionary 38500 = 167915 true remaining; existing SIPs ~30000/mo (7 funds=29993, verified in investment_transactions PURCHASE last 4 months); so new-to-deploy ~137915, NOT 127915. The plan conflated existing+new into one 'surplus' AND hid the existing-SIP line so visible parts summed 308384 but were stamped =348384 ✅. FIXES (T2, branch feat/agent-response-calibration, web+agent-harness, NO core): (A) WEALTH_INSTRUCTIONS: full-breakdown=CONSOLIDATED SINGLE VIEW (one table, no repeated numbers), NEVER ASCII-art/flow boxes, no unrequested sections, no trailing 'want me to make X' menu; ARITHMETIC-RECONCILE rule: every rupee of income is an explicit line INCLUDING 'Existing investments' as its own row, new surplus=income-fixed-discretionary-existing, keep committed vs new separate, RE-COMPUTE fresh + DO NOT TRUST remembered/profile totals, never stamp ✅ on non-reconciling numbers; plus memory-hygiene rule under Notes: store ONLY durable facts, never computed budgets/plans/projections/snapshots. (B) Markdown.tsx: added <pre> handler (single scrollable compact monospace panel) + code component distinguishes block(lang-class OR multiline)->no pill vs inline->pill; hr my-2; +3 Markdown.test.tsx (web 63 green). (C) DATA REPAIR: backed up demo.db.memory.db, rewrote mastra_resources.workingMemory to durable-facts-only (5315->1489 chars, wrong number+Perfect-balance removed) via better-sqlite3 UPDATE. STILL TODO at save time: purge/neutralize the 11 polluted mastra_messages so semanticRecall can't resurface them; re-verify live on repaired store; commit A+B; then finish-branch. GOTCHA: prompt tuning ALONE cannot beat a specific wrong number saved in memory — must clean the store.
- Response-calibration + interactive-questions session CLOSED: pushed feat/agent-response-calibration; Opus tier fixes reply quality; two follow-up features split to separate sessions — Session 2026-08-04 close-out. BRANCH feat/agent-response-calibration pushed to origin/ak688744 (HEAD 6ff2b2a), PR NOT yet opened (user call). Commits this session: ask_user tool + question SSE + QuestionChips (calibration+interactive-questions feature, T1, reviewed READY TO MERGE); 25c3129 consolidate-full-breakdown + arithmetic-reconcile prompt + Markdown.tsx pre/code whitespace fix (T2); 6ff2b2a verify scripts + gitignore backups/worktrees. MODEL FINDING (important): switching the wealth_chat route to OPUS produced markedly better replies than haiku — so calibration + arithmetic quality were partly MODEL-bound after all, not only memory pollution. Opus is ~5x haiku per token (/ vs /) but for single-user financial advice where wrong math is the real risk it's defensible; per-task routing (AI Settings) can confine Opus to wealth_chat only. STILL OPEN / PARKED: (1) poisoned Mastra memory store — mastra_resources.workingMemory was repaired (durable-facts-only, 5315->1489) but the semantic-recall VECTOR INDEX memory_messages_384_vector_idx_shadow (81 embedding rows, scope:'resource') STILL holds the wrong 127915 plan and re-injects it into new threads; user chose 'leave for now, test model first' then moved to Opus. Backup at demo.db.memory.db.bak-1785569512 (gitignored). To fully purge: wipe demo.db.memory.db OR clear the vector-index rows for the 8 polluted threads. (2) whether to make Opus the permanent wealth_chat route in AI Settings. TWO NEW FEATURES the user will build in SEPARATE sessions (see the two [Scope] decisions saved same day): A = credit-card bill upload flow; B = expense-page AI insights + notes->tags migration. Agent self-assessment (Opus) surfaced the data-gap list that motivated these; KEY TRIAGE INSIGHT saved: most 'agent gaps' are DATA-AVAILABILITY gaps not capability gaps — EPF/NPS already a supported computed asset class (just enter it), rent-mis-tagged-as-LOAN is one category rule, dependents/property/goals are durable profile facts (just tell the agent). The real BUILD items are CC parser (biggest advice blind spot), budgets, direct stocks, insurance. GOTCHAS unchanged: Node 22; pnpm via corepack 10.4.1; tsx never compile; push gh auth switch --user ak688744; Mastra LibSQL memory in demo.db.memory.db read via sqlite3 CLI.

## Scope
- NEXT FEATURE A (own session): Credit-card bill upload flow — detect CC bill/autopay txn, prompt user to upload the CC statement, parse+ingest line items — User decided 2026-08-04 to build this in a SEPARATE session (T1 likely — new parser + new ingestion surface, but reuses existing seams). MOTIVATION: the wealth agent's #1 advice blind spot is credit-card spend — today only the CC autopay lump sum (~₹5-9k/mo) is visible as one expense debit; zero category-level visibility into what that spend actually is (dining/subscriptions/shopping). This blocks accurate category budgeting + lifestyle-creep detection. FEATURE: when the system recognizes a credit-card bill / CC-autopay transaction, surface an option to the user to UPLOAD that card's bill/statement; parse it and ingest the line items as real categorized expense transactions (so the lump sum is replaced/expanded into itemized spend). ARCHITECTURE FIT (verified against current build): (1) import parser registry ALREADY exists — packages/api/src/import/registry.ts resolveParser(platform,kind), REGISTRY currently {hdfc:{expense:parseHdfcStatementXls}, groww:{holdings,transactions}}; a CC parser is a DROP-IN new module (D6 seam: zero route/orchestrator change). Model it on the HDFC expense parser (packages/core parsers + importTransactions orchestrator). (2) multipart upload endpoint pattern exists — POST /imports/expenses via lib/multipart.ts readMultipart + @fastify/multipart; a CC statement is another multipart upload. (3) categorization engine (packages/core/src/domain/categorize.ts, FROZEN — T1 if touched) auto-categorizes on import via the priority ladder. (4) expense txns already support accountId (L1.5 multi-account) so the CC can be its own account. (5) web ImportModal already launches expense import (BUG-008 fix). OPEN DESIGN Qs for the brainstorm: which CC issuers/formats to parse first (statements are PDF, not XLS — need a PDF parser, unlike HDFC's .xls; likely new dependency); how to DEDUPE the itemized CC spend against the existing autopay lump-sum debit so spend isn't double-counted (the autopay debit is the bank paying the card; the line items are the actual spend — need to net one out, mirrors the existing excludeFromSpend/self_transfer logic); how the agent 'recognizes a CC bill' (a category/rule match on the autopay txn -> surfaces upload CTA). Start: invoke myfinance-sdlc (classify, likely T1) -> brainstorming -> spec. Related: feature B (expense AI insights) c-separate; both split off from the response-calibration session 2026-08-04.
- NEXT FEATURE B (own session): Expense-page AI insights — clickable insight cards that open the assistant side-chat to resolve vague/abnormal txns, plus migrate Notes -> Tags — User decided 2026-08-04 to build this in a SEPARATE session from feature A (credit-card upload). T1 (new agent-driven surface + schema change notes->tags + new insights contract). FULL FEATURE as user described: On the Expenses page, generate AI INSIGHTS that (1) flag transactions needing MORE CLARITY — vague/unclear txn descriptions, or a NEW/unknown spending not seen before; (2) detect ABNORMAL spend — a category or period whose spend GREW vs the user's usual pattern. Insights must be CLICKABLE: clicking an insight OPENS THE AI CHAT IN A SIDEBAR (side-panel, not the full Assistant page) scoped to that insight, where the agent ASKS the user for details about the doubtful transaction(s); the user's explanations are then used to CREATE TAGS on those transactions. \n\nTAGS MODEL (replaces Notes): migrate away from the free-text  column to first-class TAGS. Tags carry structured hints the agent uses to categorize/understand expenses — e.g. one-time vs recurring, subscription; if a nuance like 'quarterly' is needed it goes in notes (so notes may persist as a secondary free-text field, but tags become the primary structured signal). Tags are both user- and agent-writable (the chat flow writes tags from the user's explanation). \n\nARCHITECTURE FIT (verified vs current build): (a)  column exists — transactions.note TEXT added in the expense-enhancements change (migration 0005) + ExpenseTransactionRepo.updateNote + PATCH /transactions/:id {note}. A tags model is a NEW table (e.g. transaction_tags) or JSON column + repo + endpoints; migration needed; decide relationship to note (keep note for free-text like 'quarterly', add tags for structured). (b) The wealth agent already has WRITE tools over the MCP write layer (add/update txns, categories, rules) and the categorize engine is frozen (T1 if touched). Tag-writing = new MCP write tool + core repo method. (c) The assistant chat harness (packages/agent-harness runChat + SSE POST /agent/chat + web useAgentChat/AssistantPage) EXISTS but is a FULL-PAGE Assistant; this feature needs a SIDEBAR/side-panel chat variant scoped with transaction context (pass the flagged txn ids + insight into the thread's first turn / working context). The ask_user clarifying-question tool (built this session on feat/agent-response-calibration — ask_user + question SSE event + QuestionChips) is directly reusable: the agent asks about a doubtful txn via ask_user, user answers, agent writes a tag. (d) Insights themselves: abnormal-spend detection can reuse expenseTxRepo.summary() byCategory/byMonth aggregation (already built for the Expenses redesign) to compare a category/period vs baseline; vague-txn detection = heuristic (deriveMerchantName returns null / no category / description not structured) + agent judgment; new-spend = category/merchant not seen in prior windows. There was an L2/spike 'AI insight slot as a data concern (text+severity+CTA)' reserved for exactly this — check the frontend-design-spike doc. \n\nOPEN Qs for brainstorm: insights computed server-side (deterministic stats) vs agent-generated (LLM over data) vs hybrid; the sidebar-chat UX (new component vs reuse AssistantPage in a drawer); tags schema (table vs JSON, controlled vocab vs free tags, one-time/recurring/subscription as enum tag-types); how tag writes flow from chat (agent write tool with user confirmation). Start: myfinance-sdlc (T1) -> brainstorming -> spec. Reuses this session's ask_user/question-chips machinery. Related: feature A (CC upload), response-calibration session 2026-08-04.

## Insights-bugfix
- Fixed stale/unconfident triage-cache verdicts (T2) — Root cause: a fallback verdict (from a failed/dropped LLM triage call) was persisted to expense_insight_triage forever with no re-check, so a transient failure could permanently freeze a wrong needs_input card even after the txn was tagged/categorized. Fix (branch feat/agent-response-calibration, commit 9b2bc7d, pushed to PR #16): (1) packages/agents/src/insights/triageInsights.ts - new exported applyCadenceBackstop(verdict, event) re-checks the deterministic cadence-tag backstop against ALREADY-triaged verdicts (no LLM call); TriagedInsight gained a confident:boolean field (true=genuine model judgment or backstop-forced, false=fallback placeholder). (2) packages/api/src/lib/insightsService.ts getTriagedInsights - on every cache read, heals each cached verdict via applyCadenceBackstop and persists the healed version back if it changed (so the DB row itself stops being wrong, not just the in-memory response); on fresh triage, only persists confident verdicts (unconfident fallbacks are served but left uncached so next load retries). Regression tests added in both packages (agents 12 tests incl 4 new re confident/applyCadenceBackstop; api added a 2-test describe block reproducing the exact bug + persistence-of-heal). Verified live against real demo.db: the reported May txn (UPI-...-123916535575-INVESTMENT, tags=[recurring,sip]) card now correctly suppressed on read, cache row persisted with tier=suppress confident=true. ~102 other pre-fix cache rows in demo.db lack the confident field - NOT bulk-migrated, they self-heal lazily the next time each month's insights are viewed (no LLM call for backstop-eligible ones). Full suite green: core 279 (Groww 6/6 unchanged) + agents 49 + api 94 + mcp 74 + web 70, tsc --build clean. Classified T2 (scoped bugfix, no core financial logic, no new architecture) via AskUserQuestion.

## Data-source-research
- Evaluating RapidAPI Indian MF APIs for holdings/sector/expense/risk fundamentals — MyFinance needs MF fundamentals beyond NAV; indianapi.in only gives NAV+returns+rating, so checking RapidAPI marketplace options

## Workflow
- Investment Analyzer spec written; committing pending CC-split branch changes before merge — Brainstormed+wrote spec docs/superpowers/specs/2026-09-06-l4.1-investment-analyzer-agent-design.md for the L4.1 Investment Analyzer agent (v1 health analysis). Meanwhile found 442 uncommitted lines on feat/credit-card-bill-split beyond the spec: (1) the needs_clarity per-txn hybrid + merchantKeyOf ref-number-stripping fix in packages/core/src/domain/insights/{expenseInsights,consolidateInsights}.ts + tests (T2 core insights change, matches prior memory decisions 0b91cf7d/b5bc154d), (2) CC-split UI/API follow-ups in ExpensesPage.tsx/transactions.ts + tests, (3) CLAUDE.md/copilot-instructions doc updates. User chose to commit these (Option 2) with per-body messages after verifying tests green, then merge the PR, then start Investment Analyzer fresh on main.

---

## How to Save Decisions (for Copilot)
When the user makes an architectural decision, tech stack choice, convention, or
discusses a constraint during this session, save it by appending a JSON line to
`.ai-memory/decisions.jsonl` in this exact format:
```json
{"id":"<8-char-random-hex>","ts":"<ISO8601-timestamp>","category":"architecture|constraint|convention|testing|scope|unresolved","decision":"<one clear sentence>","rationale":"<why this was decided>","confidence":1.0,"source":"copilot"}
```
After saving, regenerate this file by reading ALL decisions from `.ai-memory/decisions.jsonl`,
grouping by category, and reproducing this exact format including this instruction section.

---

## Research Findings Index (168 entries)
| Topic | Tags | Staleness | Date |
|-------|------|-----------|------|
| Indian MF holdings disclosure — ground truth | mf-holdings, sebi, amfi, amc, opendata | stable | 2026-09-05 |
| AMFI portfolio disclosure is link aggregator not host | mf-holdings, sebi, amfi | stable | 2026-09-05 |
| SEBI monthly portfolio disclosure clause found | mf-holdings, sebi, amfi | stable | 2026-09-05 |
| Advisorkhoj FINAL verdict | advisorkhoj, mf-data, external-api, verdict, holdings, sector | stable | 2026-09-05 |
| Advisorkhoj real urls | advisorkhoj, mf-data | stable | 2026-09-05 |
| ICICI monthly portfolio CONFIRMED ZIP | mf-holdings, amc | stable | 2026-09-05 |
| Advisorkhoj bad url | advisorkhoj, mf-data | stable | 2026-09-05 |
| Morningstar FINAL verdict | morningstar, mf-data, external-api, verdict, auth | stable | 2026-09-05 |
| ICICI official file URL not indexed | mf-holdings, amc | stable | 2026-09-05 |
| Bing broke hyphen slug->movie | mf-holdings, sebi, amfi | stable | 2026-09-05 |
| Morningstar bad fund id | morningstar, mf-data | stable | 2026-09-05 |
| Axis MF monthly portfolio CONFIRMED consolidated XLSX | mf-holdings, amc | stable | 2026-09-05 |
| Bing functional, need slug query | mf-holdings, sebi, amfi | stable | 2026-09-05 |
| ValueResearch FINAL verdict | valueresearch, mf-data, external-api, verdict, paywall | stable | 2026-09-05 |
| ICICI blob download path found | mf-holdings, amc | stable | 2026-09-05 |
| searx antibot | mf-holdings, sebi, amfi | stable | 2026-09-05 |
| VR 301 redirect | valueresearch, mf-data | stable | 2026-09-05 |
| ICICI/Axis JS-gated portfolio lists | mf-holdings, amc | stable | 2026-09-05 |
| AMFI SPA nav no hrefs | mf-holdings, sebi, amfi | stable | 2026-09-05 |
| Tickertape FINAL verdict | tickertape, mf-data, external-api, verdict, holdings, sector, risk | stable | 2026-09-05 |
| All search engines blocked | mf-holdings, sebi, amfi | stable | 2026-09-05 |
| Tickertape holdings PUBLIC works | tickertape, mf-data, holdings, sector, external-api | stable | 2026-09-05 |
| SBI portfolios CONFIRMED consolidated XLSX | mf-holdings, amc | stable | 2026-09-05 |
| Tickertape mfId format | tickertape, mf-data | stable | 2026-09-05 |
| marginalia redirect | mf-holdings, sebi, amfi | stable | 2026-09-05 |
| archive.org blocked, SEBI guess 404 | mf-holdings, sebi, amfi | stable | 2026-09-05 |
| Tickertape holdings route valid | tickertape, mf-data, holdings, external-api | stable | 2026-09-05 |
| SBI /portfolios and ICICI statutory-disclosures paths | mf-holdings, amc | stable | 2026-09-05 |
| Ecosia 403 too | mf-holdings, sebi, amfi | stable | 2026-09-05 |
| Tickertape soft404 empty | tickertape, mf-data | stable | 2026-09-05 |
| Mojeek 403; recall Reg 59A | mf-holdings, sebi, amfi | stable | 2026-09-05 |
| Google CAPTCHA on ICICI/SBI queries | mf-holdings, amc | stable | 2026-09-05 |
| Tickertape MF NEXT_DATA rich | tickertape, mf-data, holdings, sector, risk | stable | 2026-09-05 |
| Bing returns static generic SEBI set | mf-holdings, sebi, amfi | stable | 2026-09-05 |
| Axis portfolios JS accordion; ICICI mandatory-disclosures 404 | mf-holdings, amc | stable | 2026-09-05 |
| Tickertape MF page keywords | tickertape, mf-data, holdings, sector | stable | 2026-09-05 |
| Tickertape search stocks only | tickertape, mf-data | stable | 2026-09-05 |
| master keyword triggers film | mf-holdings, sebi, amfi | stable | 2026-09-05 |
| Axis downloads no portfolio; ICICI search noise | mf-holdings, amc | stable | 2026-09-05 |
| Tickertape API host live | tickertape, mf-data, external-api | stable | 2026-09-05 |
| Master circular URL guess 404 | mf-holdings, sebi, amfi | stable | 2026-09-05 |
| mfapi.in confirmed free NAV source | mfapi, mutual-funds, nav, data-source | stable | 2026-09-05 |
| Groww FINAL verdict COMPLETE | groww, mf-data, external-api, verdict, holdings, sector, risk | stable | 2026-09-05 |
| DDG lite captcha too | mf-holdings, sebi, amfi | stable | 2026-09-05 |
| Axis MF portfolio URLs found | mf-holdings, amc | stable | 2026-09-05 |
| RapidAPI listing pages are SPA-unreadable by WebFetch | mutual-fund, api, rapidapi, data-source, limitation | stable | 2026-09-05 |
| SEBI GET search JS-only | mf-holdings, sebi, amfi | stable | 2026-09-05 |
| Groww HOLDINGS+SECTOR confirmed | groww, mf-data, external-api, holdings, sector, risk, verdict | stable | 2026-09-05 |
| ICICI 404 Axis portfolios link probe | mf-holdings, amc | stable | 2026-09-05 |
| Bing returned film Master results | mf-holdings, sebi, amfi | stable | 2026-09-05 |
| Groww NEXT_DATA parse retry | groww, mf-data | stable | 2026-09-05 |
| RapidAPI Indian MF listings verify | mutual-fund, api, rapidapi, data-source | stable | 2026-09-05 |
| HDFC MF monthly portfolio CONFIRMED | mf-holdings, amc | stable | 2026-09-05 |
| Circular-number Bing query no hit | mf-holdings, sebi, amfi | stable | 2026-09-05 |
| Groww page embeds holdings+sector+risk | groww, mf-data, external-api, holdings, sector, risk | stable | 2026-09-05 |
| Groww detail paths probe | groww, mf-data, external-api | stable | 2026-09-05 |
| AMFI research nav no portfolio link | mf-holdings, sebi, amfi | stable | 2026-09-05 |
| HDFC MF monthly portfolio path | mf-holdings, amc | stable | 2026-09-05 |
| Groww search works | groww, mf-data, external-api | stable | 2026-09-05 |
| Bing site filter ignored | mf-holdings, sebi, amfi | stable | 2026-09-05 |
| Indian MF holdings free sources conclusion | mf-holdings, github, opendata | stable | 2026-09-05 |
| Kuvera FINAL verdict | kuvera, mf-data, external-api, verdict, holdings-check | stable | 2026-09-05 |
| AMFI portfolio URL guess 404 | mf-holdings, sebi, amfi | stable | 2026-09-05 |
| Bing works but generic SEBI hits | mf-holdings, sebi, amfi | stable | 2026-09-05 |
| Kuvera v5 detail RICH | kuvera, mf-data, external-api, expense-ratio, returns, risk | stable | 2026-09-05 |
| Bing generic banking noise | mf-holdings, amc | stable | 2026-09-05 |
| Kuvera v5 detail works | kuvera, mf-data, external-api, holdings-check | stable | 2026-09-05 |
| Google search blocked too | mf-holdings, sebi, amfi | stable | 2026-09-05 |
| DuckDuckGo CAPTCHA block | mf-holdings, amc | stable | 2026-09-05 |
| Kuvera Parag code | kuvera, mf-data | stable | 2026-09-05 |
| DDG CAPTCHA blocked | mf-holdings, sebi, amfi | stable | 2026-09-05 |
| MF disclosure search engine probe | mf-holdings, amc | stable | 2026-09-05 |
| Indian MF holdings parsers | mf-holdings, github, opendata | stable | 2026-09-05 |
| Kuvera v4 list works | kuvera, mf-data, external-api, holdings-check | stable | 2026-09-05 |
| ICICI/SBI MF disclosure URL probing | mf-holdings, amc | stable | 2026-09-05 |
| SEBI MF disclosure search retry | mf-holdings, sebi, amfi | stable | 2026-09-05 |
| Indian MF free data sources | mf-holdings, github, opendata | stable | 2026-09-05 |
| Kuvera v5 list confirmed empty | kuvera, mf-data, external-api | stable | 2026-09-05 |
| Axis MF statutory disclosures page | mf-holdings, amc | stable | 2026-09-05 |
| DDG redirect for SEBI MF disclosure search | mf-holdings, sebi, amfi | stable | 2026-09-05 |
| RapidAPI MF fetch pending | mutual-fund, api, rapidapi, data-source | stable | 2026-09-05 |
| WebFetch JSON limit | webfetch-limit, mf-data, external-api | stable | 2026-09-05 |
| SEBI MF portfolio disclosure research start | mf-holdings, sebi, amfi | stable | 2026-09-05 |
| indianapi.in base URLs + auth | mutual-fund, api, data-source, indianapi | stable | 2026-09-05 |
| Kuvera search empty | kuvera, mf-data, external-api, webfetch-limit | stable | 2026-09-05 |
| indianapi.in/pricing 404 | mutual-fund, api, data-source, indianapi | stable | 2026-09-05 |
| needs_clarity detector flags already-categorized txns as needing input (conflates category-clarity with cadence) | insights, needs_clarity, cadence, expenseInsights, deriveMerchantName, triage, ux | stable | 2026-09-05 |
| indianapi.in pricing/auth pending | mutual-fund, api, data-source, indianapi | stable | 2026-09-05 |
| AMFI homepage nav (no hrefs extracted) | mf-holdings, amfi | stable | 2026-09-05 |
| Kuvera search probe | kuvera, mf-data, external-api | stable | 2026-09-05 |
| indianapi.in stock-market API MF endpoints | mutual-fund, api, data-source, indianapi | stable | 2026-09-05 |
| Kuvera list endpoint | kuvera, mf-data, external-api | stable | 2026-09-05 |
| AMFI monthly-portfolio-disclosure guessed URL 404 | mf-holdings, amfi | stable | 2026-09-05 |
| stock.indianapi.in/docs | mutual-fund, api, data-source | stable | 2026-09-05 |
| MF data API research | mf-data, external-api, holdings, sector, research | stable | 2026-09-05 |
| mfapi scheme JSON shape confirmed | mfapi, mutual-funds, nav | stable | 2026-09-05 |
| SEBI portfolio disclosure circular URL 404 | mf-holdings, sebi | stable | 2026-09-05 |
| stock.indianapi.in root | mutual-fund, api, data-source | stable | 2026-09-05 |
| Indian MF holdings disclosure research start | mf-holdings, sebi, amfi, disclosure | stable | 2026-09-05 |
| indianapi.in API products | mutual-fund, api, data-source | stable | 2026-09-05 |
| AMFI NAVAll.txt fields confirmed | amfi, navall, mutual-funds | stable | 2026-09-05 |
| Indian MF fundamentals data sources | mutual-funds, data-sources, holdings, mfapi, amfi | stable | 2026-09-05 |
| Recurring UPI rent/SIP falsely flagged as new_spend every month — root cause + fix | insights, new_spend, merchantKeyOf, expenseInsights, consolidateInsights, upi, recurring, rent, sip, bugfix | stable | 2026-09-05 |
| May-2026 needs_input insight bug investigation | insights, bugs, expense-triage | stable | 2026-08-08 |
| A2 migration 0007 tags | migration, schema, tags, drizzle, test | stable | 2026-08-04 |
| wealth-agent calibration+arithmetic follow-up (T2) | l4, agent-harness, calibration, markdown, arithmetic | stable | 2026-08-01 |
| Mastra agent.stream().fullStream chunk shapes (L4 verbosity/step-collapse work) | mastra, l4, agent-harness, streaming | stable | 2026-07-30 |
| Wealth agent "empty response after 2-3 turns" ROOT-CAUSED + fixed (error bubble + localStorage persistence) | l4, agent-harness, web, debugging, bedrock, resolved | stable | 2026-07-30 |
| Wealth agent empty-response-after-2-3-turns investigation (in progress) | l4, agent-harness, debugging, memory | stable | 2026-07-28 |
| AI SDK mock model for Mastra offline tests (L4.0 Q9) | mastra, testing, mock-model, ai-sdk, tdd, l4 | stable | 2026-07-17 |
| Mastra MastraModelOutput usage token fields (L4.0 Q7/Q8) | mastra, usage, tokens, stream, modeloutput, l4 | stable | 2026-07-17 |
| Mastra per-call/per-agent apiKey WITHOUT env (L4.0 CRITICAL Q3) | mastra, apikey, model-router, runtime-key, provider-factory, l4 | stable | 2026-07-17 |
| Mastra correct doc URLs from llms.txt (L4.0) | mastra, doc-urls, l4 | stable | 2026-07-17 |
| Mastra models page 404 again (L4.0) | mastra, 404, l4 | stable | 2026-07-17 |
| Mastra model-providers page 404 (L4.0) | mastra, 404, model-providers, l4 | stable | 2026-07-17 |
| Mastra MastraModelOutput ref URL 404 (L4.0) | mastra, modeloutput, 404, l4 | stable | 2026-07-17 |
| Mastra package versions + Node floor + MastraModelOutput (L4.0) | mastra, versions, node, packages, l4 | stable | 2026-07-17 |
| Mastra agent.stream() return shape + usage (L4.0) | mastra, stream, textstream, usage, l4 | stable | 2026-07-17 |
| Mastra agent.generate/stream options + usage (L4.0) | mastra, generate, stream, usage, tokens, l4 | stable | 2026-07-17 |
| Mastra Memory full config: working memory + semantic recall + history (L4.0) | mastra, memory, semantic-recall, working-memory, libsql-vector, embedder, l4 | stable | 2026-07-17 |
| Mastra Memory + LibSQLStore wiring (L4.0 partial) | mastra, memory, libsql, storage, l4 | stable | 2026-07-17 |
| Mastra MCPClient stdio wiring (L4.0) | mastra, mcp, mcp-client, stdio, l4 | stable | 2026-07-17 |
| Mastra Agent construction + model string form (L4.0) | mastra, agent, model-router, l4 | stable | 2026-07-17 |
| Claude Agent SDK: Complete TypeScript Integration Guide (query, MCP, permissions, cost, Bedrock, sessions) | agent-sdk, query-api, mcp-servers, createsdkmcpserver, stdio-transport, permissions, bedrock-auth, cost-tracking, token-usage, sessions, multi-turn, canusetool, system-prompt | stable | 2026-07-17 |
| Claude Agent SDK TypeScript API — query(), MCP, permissions, Bedrock | agent-sdk, query, mcp-servers, canusetool, permissions, bedrock, typescript, entry-point, model-selection, system-prompt | stable | 2026-07-17 |
| Claude Agent SDK TypeScript Documentation Structure | agent-sdk, typescript, mcp, query(), permissions, bedrock | stable | 2026-07-17 |
| L4 write-tools code review — findings in progress | l4, mcp, write-tools, code-review | stable | 2026-07-16 |
| L4 write-tools: exact core signatures + API reference impls for the plan | l4, mcp, write-tools, planning | stable | 2026-07-16 |
| L3-Task5-networth-tool | mcp, networth, task-5 | stable | 2026-07-14 |
| L3-MCP-Task3-core-exports-verified | mcp, task3, core-exports | stable | 2026-07-13 |
| L3-MCP-package-scaffolding | mcp, typescript, monorepo | stable | 2026-07-11 |
| Expenses table redesign — current backend/frontend state on layer/3-mcp | expenses, ui, table, redesign, api | stable | 2026-07-11 |
| branch-state | feat/ai-settings-usage, pr | stable | 2026-07-10 |
| AI usage: per-run Recent Runs table + daily-spend-by-model stacked bar (T2, on PR #8) | ai-settings, usage, per-run, stacked-chart, t2 | stable | 2026-07-09 |
| Bedrock tool input_schema bug (array root rejected) — FIXED, found in live testing | ai-settings, bedrock, bug, fixed, live-test | stable | 2026-07-08 |
| Live Bedrock adapter VERIFIED against real AWS (closes I3) — us.anthropic.claude-haiku-4-5 works | ai-settings, bedrock, verified, i3-closed | stable | 2026-07-08 |
| task-22-final-review-fixes | myfinance, api, web, validation | stable | 2026-07-08 |
| ai-usage-event-row-shape | myfinance, api, types | stable | 2026-07-08 |
| AI Settings build progress checkpoint 4 — 18 of 22 done (Phases 1-3 done, web T18 done) | ai-settings, build-progress, sdd, checkpoint | stable | 2026-07-08 |
| web-ai-hooks-implementation | web, ai, hooks, tanstack-query, task-18 | stable | 2026-07-08 |
| AI Settings build progress checkpoint 3 — 16 of 22 tasks done (Phase 1+2 done, Phase 3 api through T16) | ai-settings, build-progress, sdd, checkpoint | stable | 2026-07-08 |
| ai-settings-api-task-15 | myfinance, api, ai-settings, routes, rest | stable | 2026-07-08 |
| Task 13: Export agents surface | agents-exports, phase-2, api-phase-3-bug | stable | 2026-07-08 |
| AI Settings build progress checkpoint 2 — 11 of 22 tasks done (Phase 1 + agents to T11) | ai-settings, build-progress, sdd, checkpoint | stable | 2026-07-08 |
| Task 9 Bedrock adapter review | agents, bedrock, review, llm | stable | 2026-07-08 |
| AI Settings & Usage build progress — Phase 1 core tasks 1-4 done on feat/ai-settings-usage | ai-settings, build-progress, sdd, feat-ai-settings-usage, core | volatile | 2026-07-07 |
| BUG: confirming one AI suggestion wiped sibling ai_suggested rows (recategorize sweep) | ai-categorization, bug, recategorize, fixed | stable | 2026-07-05 |
| Gemini AI-categorization 404 — stale default model gemini-1.5-flash | ai-categorization, gemini, bug, config, logging | stable | 2026-07-05 |
| AI-categorization review fixes | ai-categorize, bugfix, react, llm-error | stable | 2026-07-05 |
| AI-assisted categorization build paused at Task 4.3 — expense-redesign not on main | ai-categorization, sdd, paused, git, expenses | stable | 2026-07-05 |
| pnpm 11.2.2 crashes on Node 20.20.2 local-run | tooling, pnpm, node, local-dev, corepack | stable | 2026-07-04 |
| Consolidate Add-Investment flow | web, investments, ux | stable | 2026-06-17 |
| L2 bug-fix push + repo hygiene | git, gitignore, layer2 | stable | 2026-06-16 |
| L2 Tasks A1-A3 complete | l2, core, repository, expense, completed | stable | 2026-06-14 |
| L2 Tasks A1-A3 expense repo query/summary implementation | l2, core, repository, expense, read-aggregation | stable | 2026-06-14 |
| Stitch MCP screen generation: timeouts are expected, screens still persist; poll get_project not list_screens | stitch, mcp, design-spike, tooling, mockups, workaround | volatile | 2026-06-13 |
| L1 Task 7: registerDb seeding regresses read-endpoints test | l1, api, seeding, categories, testing | stable | 2026-06-13 |
| L1 Task 1 txRunner status | l1, api, txrunner, task-status | stable | 2026-06-13 |
| packages/api typecheck requires core declarations built via tsc --build | api, typescript, project-references, composite, tsc-build, monorepo, L0 | stable | 2026-06-12 |
| Groww golden-master XIRR test (task 4.2) | xirr, golden-master, returns, validation, core | stable | 2026-06-12 |
| L0 autoMatchAmfiCodes force-rematch is now a no-op (known minor gap) | L0, amfi, amfiMatcher, force, known-gap, scheme | stable | 2026-06-12 |
| Drizzle unordered select reorders rows vs raw SQL — breaks units accumulation | drizzle, investmentTxRepo, ordering, units, core-port, L0 | stable | 2026-06-11 |
| pnpm + vitest require Node 20 in this repo (Node 18 fails) | node, pnpm, vitest, nvm, tooling, L0 | stable | 2026-06-11 |
| Groww golden-master oracle values for L0 XIRR re-validation | groww, xirr, golden-master, validation, L0, portfolio, fixtures | stable | 2026-06-11 |
| XIRR Newton-Raphson convergence bug fix | xirr, bugfix, newton-raphson | stable | 2026-05-23 |
| XIRR excludes SWITCH_IN/OUT from cash flows | xirr, switch, bugfix, portfolio | stable | 2026-05-23 |
| AMFI matcher Direct/Regular variant scoring fix | amfi, nav, matching, bug-fix | stable | 2026-05-23 |
| better-sqlite3 requires rebuild for Node 18 | project-memory, node, sqlite, troubleshooting | stable | 2026-04-18 |

## Research Memory

BEFORE investigating APIs, libraries, errors, or patterns:
1. Read `.ai-memory/research.jsonl` and search topic/tags for your query
2. If stable match found → use directly
3. If versioned match → check version_anchored against current; use if same, re-research if different
4. If volatile match → use as hypothesis, verify
5. If no match → proceed with research, then save finding

### How to Save Research Findings (for copilot)
When you discover API behavior, library compatibility, error root causes, performance findings,
documentation clarifications, or workarounds, save it by appending a JSON line to
`.ai-memory/research.jsonl` in this exact format:
```json
{"id":"<8-char-random-hex>","ts":"<ISO8601-timestamp>","topic":"<5-15 word noun phrase>","tags":["<1-5 keywords>"],"finding":"<concise description of what was found>","source_tool":"copilot","source_context":"<what prompted this research>","confidence":<0.0-1.0>,"staleness":"stable|versioned|volatile","supersedes":null,"version_anchored":null}
```
Staleness guide: "stable" = won't change (language behavior, math), "versioned" = tied to library version, "volatile" = may change anytime (API responses, service behavior).

## Auto-Save Rules (always active)

- **Decisions**: When a project decision is made during this session, immediately save it to `.ai-memory/decisions.jsonl`. Do not ask — save automatically and briefly confirm.
- **Research**: When you discover API behavior, library details, error root causes, workarounds, or performance findings, immediately save to `.ai-memory/research.jsonl`. Do not ask — save automatically and briefly confirm.
- **Search first**: Before investigating any API, library, error, or technical pattern, first search `.ai-memory/research.jsonl` for existing findings.
