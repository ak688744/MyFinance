# L2 Web UI — Test Screenshots

Drop screenshots here during a manual test run (gitignored).
Naming: `<screen>-<state>.png` — e.g. `expenses-uncategorized.png`,
`expenses-learn-prompt.png`, `networth-home.png`, `loans-amortization.png`.
Compare against the reference mockups in `docs/superpowers/specs/mockups/`.
