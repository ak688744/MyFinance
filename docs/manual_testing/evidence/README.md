# L2 Web UI Manual Test Evidence

Screenshots and Browser snapshots captured during the manual test run.

Naming: `<screen>-<state>.png` for visual screenshots and
`<screen>-<state>-snapshot.md` for Browser accessibility snapshots.
Compare against the reference mockups in `docs/superpowers/specs/mockups/`.
